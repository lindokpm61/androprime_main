# Model-test clips. NOT run assets. Every one shows the OLD haired likeness.

Moved here 2026-08-12 after Keith opened a clip, saw hair, and reasonably
concluded the wrong base had been used. Nothing was wrong with the run assets:
these files were simply sitting in the folder root under names that read as
current, next to the clips that actually ship.

**Everything in here predates the base swap at 2026-08-12 11:22**, when
`work/cover-img.jpg` was replaced with the shaved likeness (Replicate prediction
`jnpznc3a2nrmy0czynbt58267g`). They carry the man with hair and they always will.
They are kept because they are the evidence behind the video-model decision, not
because they are usable.

| File | What it proved |
| --- | --- |
| `cover-video-wan-lookup.mp4` | `wan-2.2-i2v-fast`, ruled out: 560x704 at 16fps, and the headline drifts |
| `cover-video-seedance-lookup.mp4` | `seedance-1-lite` at 480p, ruled out: dearer per clip than Kling standard and visibly softer |
| `cover-video-kling26-lookup.mp4` | `kling-v2.6`, the pick: 1428x1452 at 24fps, the only model that renders ABOVE the 1080x1350 target |
| `cover-video-kling26-window-take1.mp4` | `window` scene take 1: strong head turn, no passer-by |
| `cover-video-kling26-window.mp4` | `window` scene take 2, adopted: passer-by present, minimal head turn |
| `cover-final-*.mp4` | Earlier Kling 2.1 and Higgsfield scene tests, and the band-crop defect that was fixed 2026-08-11 |

**The run assets are `cover-video-<slug>.mp4` in the parent folder**, one per
topic, all minted from the shaved base. If you are looking for a clip to post,
it is not in this directory.
