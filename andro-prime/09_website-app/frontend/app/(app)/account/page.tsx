import { AppPlaceholder } from '@/components/app/AppPlaceholder'

export default function AccountPage() {
  return (
    <AppPlaceholder
      eyebrow="Protected App Route"
      title="Account"
      description="This authenticated account shell is ready for profile details, result history, and support pathways once the remaining app screens are built."
    />
  )
}
