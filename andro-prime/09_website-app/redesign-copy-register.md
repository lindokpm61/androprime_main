# Redesign copy register: what the final pre-flight has to pick up

_Last updated: 2026-09-15 (**THE FINAL PRE-FLIGHT HAS RUN — Phase 4 of the Direction F migration. The `## Closed` section is no longer `_None yet._`; read it, it is at the foot of this file. Headline: the merge REMOVES fifteen HARD compliance findings and adds none — 18 HARD on `main` today against 3 on the branch, and sixteen of main's eighteen are live verdict-vocabulary defects that the branch fixes. `/membership` and `/demo`, the two largest blocks of new copy, return 0 HARD and 0 REVIEW. Ashwagandha clean across seven synonym forms. Every EFSA claim renders in its approved wording. Zero `.mdx` files changed, so row 35's "no article copy changed" is mechanically proved. **Every row now carries a disposition** and an owner (26 need sign-off, 5 need a ruling, 2 are sequencing gates, 40 passed the floor and owe a placement look, 5 owe nothing); the fragment pass ran per-article with a named source and returned 0 HARD on all 15, with zero figure-not-in-source findings. ⚠ AND THE ROW COUNT EVERY OTHER DOCUMENT QUOTES WAS WRONG: there are **78** rows (75 before this pass added three correcting ones), not 48. The `## Closed` heading had been sitting in the MIDDLE of the table since row 37, and every session since appended below it, so rows 38 to 48d — 27 rows, including 42a, 45 and the SEO commission — fell outside every count and outside the "read this table first" instruction at the top of this file. The heading is now last, where appending cannot jump it.** Earlier: **Row 48d: Keith approved the M7 SEO commission. 26 fields on 15 live articles, applied and verified in the database, and NOT visible until Direction F deploys.** **Row 48b: Keith ruled that articles get an SEO field of their own, and the corpus turns out to be SEVENTEEN articles rather than the two the route sweep could see.** `seoTitle` / `seoDescription` are new optional frontmatter keys, rendered with a fallback to `title`/`excerpt`, so an article that sets neither renders exactly what it did before. **No article copy changed and none is proposed**: what shipped is the mechanism plus `verify-article-seo.ts`, which measures all 19 articles on a dated ratchet. **Row 48c: Keith then ruled the REVIEW ROUTE, and an SEO snippet is now reviewed as metadata rather than clinically.** The commission is **29 fields, not 34** (15 titles, 14 descriptions across 17 articles; the 34 was 17 × 2 and assumed every article was over on both). **He also closed the columns question: frontmatter keys, ratified, not pending.** A revision now carries a `scope`, the database refuses `'seo'` over a changed body, and the review goes to Keith unless a mechanical guard finds the shorter line moved a claim rather than trimming one. Earlier: **Rows 48 and 48a: the whole M family of the defect register, and the first rows this file has ever carried about the `<head>`.** M1, M2, M4, M5, M6, M7. Five pages typed "Andro Prime" into their own title while the root layout appends it, so the tab said the brand twice — and `/go`, the Instagram bio link, said it twice and nothing else. **The check found a fifth instance the row had not named**, `/subscription/confirmed`, which a rendered sweep could not see because it 404s with `MEMBERSHIP_ENABLED` off. The five `/auth/*` routes had no metadata at all and rendered the root default, so login, reset and sign-up were three identical tabs at the moment a man has all three open; each now carries the heading its own card already renders. **Two meta descriptions were SHORTENED and that is the only substantive cut**: the homepage from 213 to 156, losing *"One price, nothing hidden."* (CA-026 C1, still rendered on the page) — owed to Keith. Author pages get a `metaDescription` separate from `bio`, because `bio` is also the schema Person description and should not be cut to fit a snippet; Ewa's keeps every credential. Row 48a is structure only: **eight routes were skipping a heading level under a STATE figure that said zero**, because the number was recorded and the check was not. Three of the eight were the footer, whose August fix rested on a premise about every other page. **M5 turned out to be already fixed four days before it was raised.** Two new checks: `verify-metadata.js` in `npm test` and `audit-rendered-markup.js` in `test:design:live`, each proved by reintroducing the defect. Earlier: **Rows 47 and 47a: five components and three error boundaries were still V2.0 on routes this report scores as finished, and the report now says what it measures.** Defect register C1 to C4. `route-conformance` counts the `f-` classes a ROUTE renders, so it cannot see inside a component and never asks whether the retired vocabulary is ABSENT: `BundleChoice`, `PasswordBanner`, `NewsletterForm`, `SkipToContent` and both error boundaries all scored full marks untouched, and an error boundary has no route at all, so no sweep that works from a route list has ever reached one. All rebuilt. **No sentence changed; five control labels change letter case** (row 41 is the precedent) and `global-error` gains a `lang` attribute and the company name as a mark. Four dead V2.0 components deleted. Two new checks in `npm test` ask the two questions the route count cannot — `verify-retired-vocabulary.js` (className attributes in `app/` and `components/`, dated shrinking allowlist, 3 files left) and `verify-dead-components.js` (every component has a consumer) — and each was proved by reintroducing the defect and watching it go red. Earlier: **Rows 46 and 46a: the four routes the conformance report had excluded are Direction F, and one of them turned out to be customer-facing.** `/go`, the Instagram link-in-bio grid, was excluded as "internal redirect, no UI", which describes `/go/[slug]`, the click handler, not the page every profile visitor lands on. It sat on V2.0 for the whole rebuild because a wrong reason was inherited rather than checked. No word changed on it; one typed literal now reads `SLA_COPY`. The two admin boards and the blog-preview bar are internal, carry every number and label verbatim, and lose 660 lines of inline styles to a new internal layer. 🔵 One question for Keith: the ops board keeps red/amber/green, which the 2026-09-03 ruling reserves for clinical verdicts. Earlier: **Rows 43 to 45a: the last three routes are Direction F. Conformance 33/36 to 36/36 — 100% of the measurable surface.** None of the three was a restyle. `/activate` was deprecated in June and now redirects to **`/how-to-sample`, the one genuinely new page in the rebuild**, whose five steps are transcribed byte-for-byte from the flow it replaces and whose only new copy is two sentences of standfirst; the how-to-sample **film is still owed** and the page ships without mentioning it rather than shipping the frame's empty slot (row 44a, Keith). `/not-found` keeps every word and loses two live defects. 🔴 **Row 45 is the one to read first: `/subscription/confirmed` was confirming a product that cannot be bought** — it promised a letterbox-friendly box, while the only purchasable subscription is the membership, which has no physical goods. It carried no register row and no CA record, so it is rewritten from approved strings rather than restyled, and the new sentences are owed to Keith. Row 45a collapses three copies of the membership benefit list to one module, no words changed. Earlier: **Rows 42 and 42a: all five `/lp/*` landing pages are Direction F. Conformance 28/36 to 33/36 (92%).** No claim, dose, EFSA sentence or approved answer changed, and the three kit sample readouts were extracted BY SCRIPT into one module and verified token-for-token, which fixes twelve live verdict-vocabulary defects the 2026-09-02 sweep never reached (including free testosterone graded "Low" at 0.244 and 0.231). 🔴 **Row 42a is the one to read first: thirteen sentences across seven surfaces tell a buyer there is no subscription, and the auto-renew ruling made all thirteen false. One is CA-026 C1, rendered verbatim on `/kits`. None was rewritten; a new check fails the build if `MEMBERSHIP_ENABLED` goes on while any remains. Owed to Keith, then pre-flight, together with row 12a.** Earlier: **Row 41: the buy stage, `/checkout/details` and `/order/confirmed`, is Direction F. Conformance 26/36 to 28/36.** First routes in the rebuild ported from a journey frame (V, V2, W, W2) rather than decided against DESIGN.md. The CA-018 consent sentence is reproduced character for character and the 18 gate still has all three of its statements; the only text difference anywhere is letter case on two form-control labels. Row 41a corrects row 40 on two counts: `SupplementWaitlistForm` has eight call sites not six, and `/lp/collagen` and `/lp/daily-stack` are NOT blocked by the 2026-09-07 auto-renew ruling, which names three other routes. Earlier: **Rows 38 to 40: the last eight content-marketing routes and both legal documents are Direction F. Conformance 16/36 to 26/36.** No claim, range, dose or EFSA sentence changed on any of them; `/privacy` and `/terms` assert that with a new test rather than with care, and row 39a removes a fallback that served a 200 with a legal heading and no legal document under it. Row 38a is a live copy defect found and deliberately NOT fixed: `/supplement-waitlist` promises a form control that does not exist. Earlier: **Row 37: a contrast fix on every dark panel site-wide, no words changed.** The SystemAlert action bar measured **2.39:1 against 4.5** on the line that says *call 999*; cause was specificity, and an audit across 16 routes found 12 failing text nodes in 4 places, now 0. Earlier: rows 35 to 36, **the blog rebuilt in Direction F**, which overturns the 2026-08-27 ruling; no article copy changed. Earlier still: rows 32 to 34, the public `/membership` page.)_

**The ruling this file exists to serve (Keith, 2026-09-02):** the redesign work on
`redesign/direction-f` is in test and **is not subject to copy pre-flight rules at each step**. The
pre-flight runs once, at the final stage, and picks up everything that needs changing or
re-approving. This is the same shape as the CA-045 ruling of 2026-09-01: **the gate governs
SHIPPING, not creating**, and this branch deploys nothing.

🔴 **THE RISK THE RULING CREATES, AND THE ONLY REASON THIS FILE EXISTS.** Deferring the gate is
fine. Deferring the gate with no record is not, because "we will catch it at the end" silently
becomes "we no longer know what changed". A pre-flight run against the finished branch can only
find what it can see: it reads the copy that is there, not the copy that was moved, shortened or
dropped along the way, and not the questions that were parked. So every step that touches
customer-facing words gets one line here, and the final pre-flight reads this file first.

**What belongs here:** copy moved, shortened, cut, or newly written; a claim whose container or
placement changed; an approved block that was relocated; a clinical or terminology question parked
rather than answered. **What does not:** pure design and layout changes that leave the words alone
(typography, spacing, containment, colour), which need no pre-flight at all.

---

## Open items

| # | Surface | What changed | Words changed? | Owed to |
| --- | --- | --- | --- | --- |
| 1 | `/kits` | The "what arrives in the post" tick list moves from three per-card copies to one line under all three cards. **Built 2026-09-02.** | **No.** Placement only, and it was identical on all three cards. | Pre-flight, sanity check only |
| 2 | `/kits` | **RESOLVED 2026-09-02 by tracing each orphaned field.** Five fields per kit came off the card. Three already have homes: `resultsTo` is absorbed by the new trust line; the conditional (`footLabel`/`footBody`) is covered by `/kits/testosterone`'s "The next step" plus its CA-026 D+ conformity line, and `/how-it-works` states the 12 nmol/L threshold three times; the blurb's ROUTING sentence ("Kit 2 is the better fit") is enforced harder at `/kits/testosterone:312`, a CA-025 scope block marked DO NOT REPOPULATE, and restated on `/kits` by the three parallel `who` lines. **Two are genuinely homeless: the blurb's explanatory half and `rightFor`.** Proposal: render `PANEL_MARKERS[id].why` in the panel alongside `measures`, which is existing approved copy that does the explanatory job better and is currently rendered nowhere on `/kits`; `rightFor` goes to the detail pages in the next pass. | **No new words** under the proposal: `why` is existing customer-facing copy in a new placement. | Pre-flight, placement check |
| 3 | `/kits` | **Built 2026-09-02.** The C1 "One price. Nothing hidden." panel moves from before the kit choice to after it. | **No.** CA-026 C1 carried verbatim, container and position only. | Pre-flight, placement check |
| 4 | `/kits` | **Built 2026-09-02.** Section 01 gains a standfirst: "Nine markers. Three ways to buy a slice of them. Every kit reads on the same lab, in the same units, so a result from one is comparable with a result from another." | **Yes, new.** Written for Frame O2. | Pre-flight |
| 5 | `/kits` | **Built 2026-09-02.** The panel instrument renders each marker's `measures` string from `lib/kits/panel.ts` in a new position (section 01 rather than inside cards). | **No.** Existing customer-facing strings, new placement. | Pre-flight, placement check |
| 6 | All kit surfaces | 🔴 **FAI verdict wording contradicts itself across two documents.** PRODUCT.md says "Not interpreted" was invented and the engine never returns it, and that the six allowed words come from the `BADGES` map, which yields "Reported". `/kits/hormone-recovery`'s header comment says the product's "Not interpreted" beats the frame's "Reported" because it is a clinical ruling (Ewa, `thresholds.md` item 8). Both cannot be current. | n/a, a ruling is missing | **Ewa.** Frame O2 renders no verdict, so it sidesteps rather than settles this |
| 7 | `/kits`, `/test-selector` | Known pre-existing drift, carried as found and flagged in Frame O: "answer 3 questions" and "takes less than a minute" both describe a selector that is now five steps. | **Not yet.** Fixing it is new copy. | Pre-flight |
| 8 | `/kits` | Kit card photographs `img-3`, `img-6`, `img-7` now appear on `/kits` as well as `/`. Already on the CA-045 register; this is a second placement of the same assets, not a new asset. | n/a | CA-045 signers, as a note |
| 9 | `/kits` | Each light card carries a one-line "who it is for", taken **verbatim from the homepage's own kit cards**, matched by slug ("If the question is testosterone." etc). | **No.** Existing live copy, new placement. | Pre-flight, placement check |
| 10 | `/` (homepage) | 🔴 **Parked, not answered: whether that line owes the 30-day width of the offer window.** It is drafted without it, on the reading that the window is a TERM and belongs on the terms page, not in homepage fine print. The consequence of the omission is real: a man who declines and returns three months later finds no offer and was not told that could happen. | n/a, a ruling is missing | **Keith**, then pre-flight |
| 11 | `/` (homepage) | ⚠ **Row 12 describes a route that is currently dark.** `/membership` is behind `MEMBERSHIP_ENABLED`, which is unset, so it 404s. The line must not ship ahead of the flag, or the homepage promises an offer that cannot be made. | n/a, a sequencing gate | Whoever merges the branch |
| 12 | `/` (homepage) | **Built 2026-09-02.** The free layer gains a fine-print line naming the membership for the first time on an acquisition surface: "Holding that record over time is an optional membership. It is offered once your first result is back, never before, and you never need it to buy a kit or to read your own results." It exists because the section's approved lede ("what you pay for is the record") alludes to a charge this page is forbidden to sell, so the page named a paid thing and then went silent on it. Shape taken from the approved `/kits` C1 negation ("no subscription unless you choose one"); the words are new. No price, no CTA, no benefit list. | **Yes, new.** | Pre-flight |

| 12a | `/` (homepage) | 🔴 **ROW 12'S LINE IS NOW FALSE, AND NOTHING RE-OPENED ROW 12 WHEN IT BECAME FALSE. Owner: Keith, then pre-flight.** The line reads *"It is offered once your first result is back, never before, and you never need it to buy a kit or to read your own results."* That describes the opt-in model `01_strategy/2026-09-07-auto-renew-at-day-30.md` **superseded five days after the line shipped**: under the adopted ruling the first 30 days are **included in the kit price** and the card is **charged GBP 47 automatically on day 31**, with no re-consent step. So the homepage's only membership sentence tells a cold buyer the offer comes later and is optional, on the page whose kit cards are the entry to a purchase that now starts a subscription. It understates a recurring charge, which is the precise failure section 2 of that ruling cites the Numan ASA ruling for, and it is wrong **however row 32a resolves**: 32a governs whether the PRICE line ships, and this is a different sentence making a different claim. Found at wrap 2026-09-10, verified against `frontend/app/(marketing)/page.tsx:678` (unchanged since 2026-09-02) and against the absence of any "Cancel anytime" string outside `/membership`. **Owed: a rewritten line, then pre-flight.** ⚠ **Row 10 is superseded by this row**: its parked question was whether the line owes the 30-day window width, and the disclosure it actually owes is the renewal. | **Yes, a rewrite.** | **Keith**, then pre-flight |

| 13 | `/` (homepage) | **Built 2026-09-03.** Keith ruled one section grammar across the F pages and it is `/kits`' labelled one, so the homepage's four heading sections gain a `.f-blab` mono label each: **"The two ranges"** (02), **"Where to start"** (04), **"What's free"** (05), and **"No conflict"** (06, the `.f-blab-lg` lead label on the inverted receipt). Sections 01 and 03 open with an object and take no label. ⚠ Row 12's line sits in section 05, whose new label inherits its prohibitions: **"What's free" is a section name, not an offer**, and carries no price, no CTA, no benefit list, no "join", and does not lead on the trend or the series. ⚠ This row also **supersedes the standing note in two places** (`page.tsx` and `DESIGN.md`) that the invert panel's lead label was "deliberately absent pending a pre-flight"; it was a statement about what a redraw may do, and a critique later misread it as a design ruling. | **Yes, new.** Four labels, two to three words each. No claim in any of them. | Pre-flight |
| 12b | `/` (homepage) | ⚠ **Pointer correction, no copy change.** The disclosure line's own code comment says it is "registered as item 9"; the row describing it is **item 12**, and item 9 is the `/kits` "who it is for" lines. The comment was committed as found in `c746368` and corrected here rather than silently edited. | **No.** | Nothing owed |

| 14 | `/` (homepage) | **Built 2026-09-03.** The three kit cards said **"Testosterone"**, **"Energy & Recovery"** and **"Hormone & Recovery"**; every other surface says **"Testosterone Health Check"**, **"Energy & Recovery Check"** and **"Hormone & Recovery Check"** — `/kits`, the detail-page breadcrumbs, `lib/pricing.ts` (so the checkout), the account, the activation flow and the CIO payload. The homepage was the only dissenter, one click from the page it disagreed with. All three now read from `lib/kits/names.ts`, which already called itself the single source of truth and had **one** consumer. ⚠ The two narrow homepage cards' heading now wraps to two lines at 1440 (one line at 390); cards stay equal height, no overflow. | **Yes, changed** — but TO the already-approved long form, not to new words. The short forms were the unapproved variant. | Pre-flight, consistency check |
| 15 | `/kits` | **Built 2026-09-03.** The three card CTAs said **"Order"**; they now say **"Start a baseline"**, the homepage's label for the same action on the same three products. "Order" also over-promised: it leads to a product page, not a basket. | **No new words.** Existing live approved copy in a new placement. | Pre-flight, placement check |
| 16 | All kit surfaces | ⚠ **Not fixed, recorded so the pre-flight knows the scope.** The three product names have **66 hardcoded literal occurrences across 21 files**. Rows 14 and 15 route `/` and `/kits` through `lib/kits/names.ts`; the other 19 files still restate the strings, including `lib/pricing.ts`, `lib/account/getAccountData.ts`, `lib/account/exportResults.ts`, `lib/activate/getKitActivation.ts`, `lib/content/kitCTA.ts` and the three `/lp/*` pages. They all currently AGREE, which is exactly why the divergence was invisible until the homepage broke it. | n/a, a consolidation | Whoever picks up the naming sweep |

| 17 | `/kits` | **Built 2026-09-03, and it is here despite changing no words, deliberately.** Keith ruled one recommendation across both pages: **Kit 3 is the default**, so `/kits` moves from solid buttons on Kits 1 and 3 to a solid button on **Kit 3 alone**. Previously the two pages recommended different kits one click apart, and only the homepage had a commercial reason for its choice; `/kits`' was written in its code as a layout-balance rule. ⚠ **Why a design-only change is registered anyway:** it steers toward the **£179** product, and fair-presentation questions are asked of the whole presentation, not only of the words. The argument for it is coverage, not price (Kit 3 is 9 of 9 markers, and the homepage argues the full panel is the honest default for a reader who does not yet know his question), but the pre-flight should see it rather than discover it. | **No words changed.** One boolean per card. | Pre-flight, presentation check |

| 18 | `/kits` | **Built 2026-09-03, no words involved, registered because it moves an OPEN compliance question onto a second page.** The `HeroField` data layer now renders in the `/kits` hero as well as the homepage, so the two pages share one ground. It draws real range geometry from `04_products/results-engine/thresholds.md`, and **CA-045 questions 6 and 7 are open against exactly that** ("hero data field: display or texture", and "the hs-CRP and SHBG rows the page never shows"). Nothing is labelled, named or legible, and at 0.34 opacity with the per-row fade no individual band is readable, so the reading is texture-whose-source-is-real; that reading is not ours to ratify. Safe to build under the 2026-09-01 ruling that the gate governs SHIPPING not creating. ⚠ **What the answer has to cover has changed, and changed AGAIN on 2026-09-03**: the field now renders on `/`, `/kits`, `/kits/testosterone`, `/kits/energy-recovery` and `/kits/hormone-recovery`. **FIVE surfaces, and the packet as drafted describes one.** The three detail pages took it in the same pass that caught them up with the branch; they are the pages a reader arrives at with intent to buy, so if the reading of this layer is ever anything other than texture, these are the three surfaces where it matters most. | **No words.** | **CA-045 signers**, as a scope note before the packet is sent |

| 19 | `/kits/testosterone`, `/kits/energy-recovery`, `/kits/hormone-recovery` | **Built 2026-09-03.** The three kit detail pages were rebuilt in Direction F on 2026-08-29/31 and then stood still while `/` and `/kits` took four more commits, so they were Direction F pages a generation behind Direction F. Caught up in one pass: the shared `HeroField` ground, `.f-herogrid` in place of a raw Tailwind grid, `f-sec-hero` in place of an inline `paddingTop` (Kits 2 and 3 never got Kit 1's consent-banner fix, so at 390 their order buttons sat under the banner), the `SectionRule` spine on Kits 2 and 3 (which had NO measurement device while every other F page carried it), the `.f-pstrip` coverage strip on Kits 2 and 3, and Kit 1's containment move ported to Kit 2 (the opening argument left its tray). **Two photographs were placed**: `img-7` on Kit 2 and `img-3` on Kit 3, matched by slug to the assets those same two kits already use on `/` and `/kits`. Both are already on the CA-045 register; this is a third placement of the same assets, not a new asset. Alt text and captions are carried **verbatim from `/kits`** rather than re-derived, for the reason the 2026-09-02 packet established: `img-5`'s alt described the opposite of its photograph when it was written from a filename. | **No new words.** Existing approved strings in new placements, and one block of existing prose moved out of a card into the same page's grid. | Pre-flight, placement check; **CA-045 signers** as a note on row 18's widened scope |

| 20 | All three kit detail pages | **Built 2026-09-04.** The CA-026 D+ conformity line moves from a plain white `.f-tray > .f-core` into `.f-invert`, the ink panel `/` and `/kits` already use for the same argument. Measured before the change, `.f-invert` count across the five F routes was **4, 4, 0, 0, 0**: the claim PRODUCT.md names as the brand lead was in the loudest container on the two pages that sell nothing and the quietest on the three that take £99 to £179. Each page now spends its ONE permitted invert. ⚠ The sentence is split across `.f-invert-h` and `.f-invert-p` **at a sentence boundary with the order preserved**; Kit 2's split falls one sentence later than Kit 1's because its mono label IS its first sentence and splitting earlier set the identical string twice. A mechanical check now asserts every CA-026 sentence is still present in the rendered JSX on all three pages. | **No words changed, none added, none removed, none reordered.** Container and element split only. | Pre-flight, placement check |
| 21 | All three kit detail pages | **Built 2026-09-04.** Each hero gains a `.f-price` element reading `£{PRICING.KIT_N.rrp}` from `lib/pricing.ts`. Before this there was **no price element on any kit page**: `.f-price` / `.f-kprice` / `.f-prow-p` counted 3 on `/` and 6 on `/kits` and 0 here, so the number existed only as a substring inside the button label and in one FAQ answer, on the page that takes the money. The same price was measured in **8 distinct type treatments** across the five pages; it now renders at 35.2px/700 here and on `/kits`, from one class. | **No new words.** An existing number, from the existing source module, in a new element. ⚠ It sits directly above a button whose approved label already contains the price, so £N appears twice in the hero. Flagged rather than fixed: shortening the button label is a copy change. | Pre-flight, placement check |
| 22 | `/kits/energy-recovery` | 🔴 **Built 2026-09-04, and this is the row that needs the most attention.** The hero sample report is rebuilt from the single-bar `.f-rep` into the **two-range `.f-mk` / `.f-track` / `.f-band` / `.f-you` device the homepage uses**, because `/` opens on "Two ranges. Nine markers. You should see both" and the page that sells showed one bar, no lab band and no needle. **Three of the four rows are carried verbatim from `/`** — Vitamin D 58, Active B12 45, Ferritin 62 — values, band geometry and verdicts included, so they add no new clinical assertion. **hs-CRP is the one new row**, derived from thresholds.md (`≤1` normal, Vitall reference `<1.00 mg/L`, the match recorded at thresholds.md line 63) on a 0–10 scale; **its two bands coincide**, which is the weakest visual claim the device can make. ⚠ **Every row is lab-normal by construction**, so `f-v-lab` carries the string "Lab normal" on all four, byte-identical to `/`; a value the lab would call out-of-range would have needed a second lab-verdict string that exists nowhere in the approved set. ⚠ **The result is now MIXED (two In range, two Monitor), reversing an all-flagged demonstration** (Keith, 2026-09-04): the previous four rows read Action needed / Monitor / Monitor / Monitor, so the demonstration of the product was a man for whom nothing is fine. Both Monitors are genuine splits, so the page still shows the product finding what a standard report misses. The panel also **gains "Nothing here is a diagnosis"**, carried from `/`, which it did not previously carry while drawing bands and verdicts. | **Values changed** (Vitamin D 44→58, B12 61→45, hs-CRP 1.2→0.8, Ferritin 38→62) and **verdicts changed with them**, every one re-derived from thresholds.md and stated in the source comments. Marker sub-labels and the Recommendation line are unchanged. "Nothing here is a diagnosis" is existing approved copy in a new placement. | **Ewa**, as a clinical-demonstration change, and pre-flight |
| 23 | `/kits/testosterone`, `/kits/hormone-recovery` | 🔴 **RAISED AS A BLOCKER AND WITHDRAWN THE SAME DAY. Built 2026-09-04.** This row first said the two-range readout could not be drawn for Albumin and Free Testosterone because their rules are FLOORS rather than bands (`<35 -> GP` with the upper "left open"; `< referenceLow` with no upper action), so drawing a band meant inventing a bound Ewa had declined to set. **That was wrong, and Keith caught it by pointing at ClickUp.** The ruling exists in two places: `approval-record-biomarker-bands-v2-2026-08-07.md` records **"Albumin upper band | No | No change"**, which is a decision and not an omission; and **`lib/results/classifier.ts:resolveBarZones` implements it** as `{ color: 'optimal', upTo: null }` for both markers, has done since the 2026-06-16 sign-off, and **already renders that way to customers on the dashboard**. CA-044 records the bands themselves as APPROVED; what is still pending there is the card WORDING for `high-testosterone` and `high-vitamin-d`, which these rows do not touch. So a floor draws to the end of the track, our band renders wider than the lab's at the top on those two rows, and that is the ruling drawn honestly rather than a new claim. Kit 1 now shows 5 rows, 4 tracks, 1 split; Kit 3 shows 9 rows, 8 tracks, 3 splits; FAI draws no track on both (ruling 8). ⚠ **Two Kit 3 values changed to satisfy the lab-normal constraint**: Vitamin D 47 -> 58 and hs-CRP 2.1 -> 0.8, because 47 sits below Vitall's 50-250 interval and 2.1 above its <1.00, and "Lab normal" is the only lab-verdict string that exists in the app. A consequence worth Keith's eye: Kit 3's demonstration is now milder than it was, with no Action-needed row. | **Values changed on Kit 3** (Vitamin D, hs-CRP), each re-derived and stated in the source comments. No new strings; "Nothing here is a diagnosis" is carried from `/`. | **Ewa**, as a clinical-demonstration change, and pre-flight. The BAND GEOMETRY is already ratified and is not what is being asked. |

| 24 | All three kit detail pages | **Built 2026-09-06. NEW COPY, four short labels.** Keith's 2026-09-03 ruling is one section grammar across the F pages, and `/` and `/kits` label 4 of 4 heading-led sections while the kit pages left four bare. Added: **"Questions"** above the FAQ heading on all three, and **"The founders"** above Kit 3's founders section. Both are section NAMES in the existing grammar ("The reality", "The process", "The panel") and neither carries a claim, a benefit, a marker, a threshold or a number. Measured after: 0 unlabelled `h2.f-h2` on all five routes, at 1440 and 390. | **Yes, new.** Two distinct strings, one and two words. No claim in either. | Pre-flight |
| 25 | All three kit detail pages | **Built 2026-09-06. NEW COPY in placement, but TOWARDS the approved names.** The five F eyebrows read `none / "Diagnostic kits" / "Kit 01 // Testosterone" / "Data first" / "Data first"` — so two DIFFERENT products shared an eyebrow that identified neither, one click apart, and Kit 1's identified itself with a SHORT form. They now read **"Kit 1 // Testosterone Health Check"**, **"Kit 2 // Energy & Recovery Check"**, **"Kit 3 // Hormone & Recovery Check"**. Two fixes in one: the number is **unpadded**, matching `/kits`' own `NUMBER_LABEL` and the homepage cards (Kit 1 was the site's only "Kit 01"); and the name is **read from `lib/kits/names.ts`**, not typed, so this adds no new hardcoded call site to the 66 row 16 counts. ⚠ Row 14 records that the short forms ("Testosterone") were the **unapproved variant** and the long forms are the approved ones, so this moves Kit 1's eyebrow onto approved copy rather than away from it. Verified: none of the three wraps at 390. | **Changed, but TO the already-approved long form.** "Data first" is retired from two pages. | Pre-flight, consistency check |
| 26 | `/kits/hormone-recovery` | **Built 2026-09-06. Completes row 15's sweep.** The comparison table's footer CTAs for Kit 1 and Kit 2 said **"Order"** — the last two instances on the site. Row 15 removed that label from `/kits`' cards on 2026-09-03 for two reasons that both apply here: it is not the label `/` and `/kits` use for the same action on the same products, and it over-promises, because the link leads to a product page and not to a basket. Now **"Start a baseline"**, with the pip. Measured after: 0 "Order" CTAs across all five routes at both widths. | **No new words.** Existing approved copy replacing a label that row 15 already retired elsewhere. | Pre-flight, placement check |
| 27 | `/how-it-works` | **Built 2026-09-06. NEW COPY, one photo caption: "Any postbox, any morning".** The page had no photograph at all, so it had no caption convention to inherit; this follows the seven that exist ("Ordinary Tuesday", "Thursday, 4pm", "No email, no gate"): a place and a time, no claim, no product, no number. It sits under `img-8` in the hero. | **Yes, new.** Four words. No claim. | Pre-flight |
| 28 | `/how-it-works` | **Built 2026-09-06. Completes rows 15 and 26.** The three kit cards' CTAs said **"Order Kit 1 / 2 / 3"**. Those were the last three "Order" labels on the site and nobody had counted them, because the two earlier sweeps were scoped to `/kits` and the detail pages. All three now read **"Start a baseline"**, which is what `/` and `/kits` say for the identical link to the identical destination. | **No new words.** Existing live copy, matched by destination, replacing an unapproved variant. | Pre-flight, consistency check |
| 29 | `/demo` | **Built 2026-09-06. NEW COPY, the demonstration notice.** "These results are a sample. They do not belong to a real person, and no customer's data is used here. Everything else on this page is the report you would get: the same laboratory ranges, the same action bands, and the same explanations." Not dismissible, above the fold on every scenario, because a reader arriving from a shared link mid-page must still be able to tell whose numbers these are. The second sentence is the load-bearing one and it is a claim about the product, so it has to stay true: if the demo ever stops rendering through the real classifier, this line becomes false. | **Yes, new.** | **Ewa + Keith**, CA-046 q1 |
| 30 | `/demo` | 🔴 **REWRITTEN TWICE ON 2026-09-07; READ THE LAST PARAGRAPH FIRST, IT IS THE ONE THAT IS TRUE.** Originally: three result-switcher labels and blurbs.** It was three result-switcher labels and blurbs -- "Lab normal, three to monitor" / "Nothing to act on" / "A result that goes to a GP" -- argued as a set so the demo could not be read as the flattering case. Keith removed the switcher: two of the three were Kit 1, a five-marker panel on the page whose job is to show the product, and *"it's not needed in the demo page"*. **What is on the page now** is the JOURNEY switcher, three labels and three blurbs, which is copy this register has not previously carried: "Day 3 &middot; waiting for the lab", "Day 14 &middot; first result, month one running", "Day 104 &middot; member, retest landed", each with one line saying what that moment is. It names a **charge date and an included 30 days**, which is membership copy on an ungated surface and is the widening `03_compliance/STATE.md` already flags. The withdrawn strings, including the verbatim "That result earns us nothing", are no longer rendered anywhere on `/demo`. <br><br>🔴 **THEN THE WHOLE ROUTE WAS REBUILT FROM THE PROTOTYPE, and this row's real scope is now much larger than either version above.** Keith: *"rebuild the demo from scratch, following page for page the prototype. Exactly."* Twelve state-and-tab cells now render, and **the copy in them is lifted from `design/prototypes/demo-account-interactive.html`, which has had no pre-flight and no clinical sign-off and whose own header forbids lifting it to a live surface without both.** New strings, by screen: **waiting** -- "Where your sample is", "Your sample is being analysed", the four tracker step labels, "Ready for when it lands", "Why analysis takes days, not hours", "What a reference range is, and is not"; **plan** -- the month-one notice headline and its five inclusion bullets, "Your one number to move", "What month one includes, and what nothing gates"; **record** -- "Your record so far", "Only our own kits feed this", "Two purchases, one picture", "Your purchases"; **account** -- "GP handoff summary", "Ask the clinician", "Member shop", "Only where we test the marker"; **marker detail** -- "What the difference means", "Movement · two points, N days apart", the provenance line, and the attribution "Andro Prime clinical logic · signed by Dr Ewa Lindo". ⚠ **Three prototype strings were deliberately NOT lifted** and the substitutions are recorded in `components/app-shell/AppShell.tsx`: its invented daily tasks ("D3 capsule", "20 min daylight") and 1-to-10 energy scale, its own six verdict words, and its "Vitamin D3 + K2 · £18.95 member price" row, which names a product that does not exist in the range. | **Yes, new, and far more of it than either earlier version of this row described.** | **Ewa + Keith**, CA-046 q3 and the new q3b/q3c |
| 30a | `/demo` | ✅ **CLOSED 2026-09-16 (Keith): THE BULLET IS REMOVED, AND THE LIST IS NOW IMPORTED RATHER THAN RETYPED.** Keith's reason: *"they go nowhere and we haven't finalized the product range yet."* The string *"Supplements at member price, only where we test the marker."* no longer renders. 🔴 **THE FIX IS THE IMPORT, NOT THE DELETION.** Deleting the string would have left the next person free to type a fifth; `AppShell.tsx` now maps `MEMBERSHIP_INCLUDES` from `lib/membership/includes.ts`, so **the canonical three-item list is the only list** and a benefit cannot be added to this surface without being added where the rule lives. ⚠ **The retest bullet above it stays hand-built on purpose**: it is an ENTITLEMENT rather than an inclusion, it carries a date, and `includes.ts` deliberately keeps it out of that array for that reason; its wording is still `MEMBERSHIP_RETEST_TERMS`, verbatim. ✅ **The account screen's "Member shop" row needed nothing** and was checked rather than assumed: it already reads *"Supplements — Not open yet · join the waitlist"*, which claims no member price and points at the waitlist, the state the range is actually in. Verified two-sided on the rendered DOM: the removed string absent, and all three canonical includes, the retest entitlement and the CA-046 Q6 qualifier all still present. `typecheck`, `test` and `build` all exit 0. <br><br>~~⚠ **ROW 30'S "THREE PROTOTYPE STRINGS DELIBERATELY NOT LIFTED" IS A FOUR-ITEM LIST REPORTED AS THREE, AND THE FOURTH IS THE ONE A WRITTEN RULE FORBIDS.**~~ **The original finding:** Found by the independent pre-flight pass, 2026-09-15; corrected here rather than edited into row 30 silently.** `AppShell.tsx` renders *“Supplements at member price, only where we test the marker.”* as the fourth bullet of the membership includes list. `lib/membership/includes.ts` holds the canonical list as **three** items and states the rule in terms: member pricing is for supplements, which have no delivery path today, and **a paywall must not list a benefit that cannot be delivered** (`01_strategy/2026-08-26-membership-offer-window.md`). The public `/membership` page obeys it and says so in its own header; `/demo` hardcodes its own five-bullet list instead of importing `MEMBERSHIP_INCLUDES`. 🔴 **The same file proves the benefit cannot be delivered ~250 lines later**, recording that every supplement in `lib/subscriptions/products.ts` is RETIRED and pointing the shop row at a waitlist — so the contradiction is in-file, not across documents. The durable change is to import the constant rather than retype the list, which is what stops a fourth bullet reappearing. | **Yes, one string, and it should not be there.** | **Keith** (it is a business rule, not a clinical one), then pre-flight |
| 30b | `/demo` | 🔄 **REWORDED 2026-09-16 (Keith): THE MONTH-ONE NOTICE'S THREE DEMO-AUTHORED SENTENCES MOVE TO THE THIRD PERSON.** Raised by the 2026-09-16 pre-flight as the one string on the membership screens that could read as an **offer rather than a depiction**: it said *"You are a member until 13 September"* over *"On 13 September your card is charged £47 and it carries on monthly. Cancel any time before then and nothing is taken."* **A financial claim in the second person, on a page a stranger reaches with no account and nothing bought.** The demonstration notice at the top of the stage covers the RESULTS being a sample and says nothing about a charge, so it never reached this card. Now: *"He is a member until 13 September."* / *"It came with his kit. He has one number to move and N days to move it."* / *"On 13 September his card is charged £47 and it carries on monthly. He can cancel any time before then and nothing is taken."* 🟢 **"He" is not a new voice**: the stage already establishes it (*"Tap through it as he would"*, *"the panel moves him between the three states"*, *"He is inside his laboratory's range"*), so the card now matches the frame the page had already set. ⚠ **The cost, recorded as a choice rather than an oversight:** the other screens inside the phone stay second person (*"Your results"*, *"Your plan"*), because that is what the real app says and the demo's argument is that it IS the real app. **The voice is mixed inside the phone, deliberately, and only on the card that makes a claim about money.** One edit to revert. 🔴 **THE BULLETS BELOW IT WERE NOT TOUCHED AND MUST NOT BE**: the retest sentence and the includes list are approved copy carried verbatim from `lib/membership/includes.ts`, whose header states that rewording any of it is a copy decision with its own pre-flight. They remain second person, which is the deliberate seam. Verified two-sided on the rendered DOM: three new strings present, three old strings absent, **and both approved strings still present unchanged**. `scan.js` 0 HARD / 0 REVIEW; `typecheck`, `test` and `build` all exit 0. | **Yes, three sentences reworded.** No new proposition: same price, same date, same cancellation right. | **Keith** (it is a depiction-vs-offer framing question on membership copy, not a clinical one), then this pre-flight, which is run and clean |
| 31 | `/`, `/demo` | **Built 2026-09-06.** The homepage's "See the app" card was inert with an "Opening soon" label because the route did not exist; it now reads **"Open the demo"** and links to `/demo`. The card's own body copy is unchanged and still says "We never put your data in it", which the route now makes structurally true: `lib/results/demo.ts` builds from fixtures with no Supabase client and no user id on the path. Also new: the demo's closing strip ("This is what you get") restating the finger-prick, the UKAS lab and the 2-to-5-working-day turnaround, all three already approved elsewhere. | **Yes, new** for the CTA label and the closing strip; the card body is unchanged. | Pre-flight |

| 32 | `/membership` (NEW public route) | 🔴 **Built 2026-09-08, and it is the largest single block of NEW COPY on the branch.** The seventh Direction F route: a PUBLIC explainer at `/membership` that discloses the membership price and the day-31 charge. It has **no join button and cannot have one** (membership may only be joined inside the 30 days after a result, enforced server-side with a 409), so every CTA routes to `/kits`. New strings, by section: hero (**"Your first 30 days are in the kit price. Then it renews."**, its standfirst, and the chip "No join button here"); the four-step spine (**"You buy a kit" / "Your result comes back" / "Your first 30 days run" / "Day 31"** with bodies and mono footers); section 02 (**"It starts when you learn something. Not when you pay."**, two paragraphs and the pull quote **"Ninety days after a card was charged is not ninety days after a baseline."**); section 03 (**"A test on a date, not a credit in an account."** and the 90/365 explanations); section 04, the ink panel (**"£47 a month, from day 31."** and two disclosure paragraphs); section 05 (**"Three things you keep whether you pay us or not."** and three items); section 06 (**"You cannot buy this on its own. On purpose."** and three paragraphs); six FAQ pairs; and the close (**"The test comes first."**). ⚠ **CARRIED VERBATIM, not new:** the three "what membership includes" lines and the entitlement paragraph ("Included while you are a member. You need to be a member on that date...") are lifted from the in-app paywall unchanged. ⚠ **Every number is imported, not typed** (`PRODUCT_MAP.membership`, `MEMBERSHIP_OFFER_WINDOW_DAYS`, `FIRST_CYCLE_RETEST_DAYS`, `ANNUAL_RETEST_DAYS`, `PRICING`, `KIT_NAMES`), so a constant moving breaks the build rather than the copy. 🔴 **Deliberately absent, each for a stated reason: member pricing on supplements** (no delivery path, the 2026-08-26 rule), **any kit discount** (there is none, and section 05 says so), and **"watch your numbers move" as the lead** (the §1b cold-start constraint). | **Yes, a lot, and all of it new.** | **Pre-flight**, as the largest single item on this register; and **Ewa + Keith** for anything read as a clinical cadence claim (the 90-day and 365-day intervals are stated to a customer on a marketing page for the first time, though both are already customer-facing inside the app) |
| 32a | `/membership`, `/kits/*` | ✅ **RESOLVED 2026-09-16 (Keith): THE CONTRADICTION IS CLOSED, AND IT CLOSED BY NARROWING THE RULE RATHER THAN BY PICKING A WINNER.** `site-funnel-model.md` §2 carried *"Sell or price the membership"* as one absolute bullet; **it was one rule covering two different acts**, and its own stated reason (*"it is not purchasable before a result exists"*) argues about **selling**, not about stating a price. The bullet is split: **selling stays absolute and unchanged**, pricing is now governed by a test. The carve-out is written into §2 with its reasoning. **The test, which matters more than the list of surfaces:** *a surface may not SELL the membership ever, MUST price it where it can take money for a kit, and MAY price it where it demonstrably cannot take money at all.* **The obligation follows the buy button** — the discriminator Keith had already applied on 2026-09-11 without naming it, when the `/lp/` pages came in because they carry their own buy buttons and take cold paid traffic. Permitted today: the four `/kits/` routes, the three kit `/lp/` pages, and **`/demo`**. ⚠ `09_website-app/frontend/PRODUCT.md` says *"no acquisition surface may sell it"*, which is about selling only and stays true as written; checked rather than assumed. 🔴 **THIS ROW'S RESOLUTION DOES NOT REACH `/membership`** — see row 32d, which is the question this row was conflating and which is still unruled. <br><br>~~🔴 **THE TWO GOVERNING DOCUMENTS CONTRADICT EACH OTHER AND NEITHER WAS AMENDED. Owner: Keith.**~~ **The original finding, kept because the carve-out was written against it:** `07_sales/funnel/site-funnel-model.md` §2 states an absolute prohibition, *"no acquisition surface may sell or price the membership"*, listed under "What no acquisition surface may do". Keith's ruling of 2026-09-07 (`01_strategy/2026-09-07-auto-renew-at-day-30.md` §4) requires the price line *"Includes 30 days of membership. £47/month after. Cancel anytime."* on all four `/kits/` routes, for a prominence reason the DMCCA read makes close to mandatory. The ruling is later and specific, so it was taken as governing for this build, **but the funnel model was never given a carve-out note and still reads as absolute.** The decision sweep of the same day did not catch it because the sweep hunts for superseded FACTS, and the prohibition is not superseded, only narrowed. What turns on the answer: whether this page may be linked from `/` or `/kits`, and whether the §4 price line ships at all. Logged as task-observer OBS-635 with a proposed sweep change. | n/a, a ruling is missing | **Keith**, then pre-flight |
| 32b | `/membership` | ⚠ **Sequencing gate, the same shape as row 11 and it is the reason the route is flag-gated.** `2026-09-07-auto-renew-at-day-30.md` §8: the moment the redesign merges, a page reading "£47/month after" would be live while **Stripe has no `trial_period_days` anywhere in the repo** and `createMembership` charges from checkout, so the mis-statement would arrive by MERGE rather than by anyone deciding to ship it. The page is behind `MEMBERSHIP_ENABLED` and 404s with the flag off, which is that document's own recommended fix. **The flag must not be flipped until the trial mechanic exists and the membership terms are drafted.** | n/a, a sequencing gate | Whoever flips the flag |
| 32c | `/membership` | 🔴 **NEW COPY, and it is the one finding the Guardrail #1 pre-flight produced against this page.** Item 3 of the includes list, *"Ask the clinician. Questions answered every month, published for all members."*, is carried verbatim from the in-app paywall, **but the surface changed**: in-app it is read by a member who has already met the product, and here it is read cold by a visitor who has never tested. `site-funnel-model.md` §2 forbids an acquisition surface implying clinical services are live, and the Phase 0 boundary in `03_compliance/CONTEXT.md` is the rule behind it. "Published for all members" already carries the general-not-individual point, but only by implication. Added, in the footer's own already-approved words: **"Clinician answers are general and published to every member; they are not individual medical advice and they do not replace your GP."** The verbatim line itself is unchanged. ⚠ **The underlying question is still open and is not settled by this sentence:** whether the benefit may be named on a public surface at all, given `membership-F.html` records that the live member screen has only ever rendered the block EMPTY. | **Yes, new**, one sentence, built from approved footer language. | **Ewa + Keith.** This is a Phase 0 boundary question, not a wording preference |
| 32d | `/membership`, `/demo` | ✅ **RULED IN FULL 2026-09-16 (Keith): A PUBLIC `/membership` PAGE SHOULD EXIST, AND IT IS PUBLIC.** The question this row was raised to name — *"does a public priced membership page exist at all"* — has now been put and answered. 🟢 **The ruling is CONSISTENT WITH the §2 carve-out rather than an exception to it, which is worth recording because it means the test was written at the right level.** The carve-out permits pricing where a surface *"demonstrably cannot take money at all"*, and `/membership` cannot: it has **no join button and cannot have one** (membership is joinable only inside the 30 days after a result, enforced server-side), so every CTA routes to `/kits`. The test predicted this answer; Keith's ruling confirms it. 🔴 **WHAT THE RULING DOES NOT DO, AND MUST NOT BE READ AS DOING. (1) IT DOES NOT FLIP THE FLAG.** Row **32b** is a sequencing gate and it stands: the page is behind `MEMBERSHIP_ENABLED`, `notFound()`s with the flag off, and the flag defaults false. **Re-verified 2026-09-16: `trial_period_days` still appears NOWHERE in the repo except inside the comment on the membership page asserting its own absence**, and `createMembership` charges from checkout. A page reading *"£47/month after"* going live before the trial mechanic exists would be a mis-statement arriving **by merge rather than by decision**. The membership terms are also still `DRAFT, not synced live`. **Ruling the page public is a decision about whether it may exist, not about when it ships.** **(2) It does not answer row 32c's Phase 0 question**, which is Ewa's and still open: whether the *"Ask the clinician"* benefit may be named on a public surface at all, given the live member screen has only ever rendered that block EMPTY. **(3) It does not decide whether `/` or `/kits` link to it**, which was row 32a's other half. <br><br>~~🟡 **HALF RESOLVED 2026-09-16, AND THE HALF THAT MATTERS IS STILL OPEN.**~~ **The earlier half-resolution, kept because the `/demo` reasoning is still the record:** ✅ **The `/demo` half is ruled**: Keith decided the demo keeps its membership price ungated, and `site-funnel-model.md` §2 now carries the carve-out that makes it lawful under our own rules rather than an exception to them. His reasons are on the record: the demo **cannot take money** (both "Manage membership" buttons are inert, no join path, membership joinable only inside the 30 days after a result), it **hides nothing** (GBP 47 goes public on seven routes the moment the branch merges, so gating it buys no protection), and hiding it would invert the rule's purpose, contradicting his 2026-09-07 ruling that the demo *"should demonstrate everything that is available that we're planning to do"*. 🔴 **THE `/membership` HALF IS UNTOUCHED AND MUST NOT BE READ AS DECIDED.** *"Does a public priced membership page exist at all"* has still never been put to anyone. The 2026-09-07 ruling authorised one sentence on named kit routes; the carve-out authorises pricing on surfaces that cannot take money; **a standalone public explainer is neither**, and the page is built, flag-gated and indexable when the flag is on. **Do not cite the carve-out for it** — that is exactly the compressed-citation failure this row was raised to name. ⚠ **One wording item, not a gate:** the demo's plan screen says *"On 13 September your card is charged GBP 47 and it carries on monthly"* in the second person, where it describes the fictional man. <br><br>~~⚠ **ROW 32a ASKS ONE QUESTION WHERE THERE ARE TWO, AND THE SECOND IS UNRULED RATHER THAN CONFLICTED.**~~ **The original finding, which the ruling above half-answers:** Independent pre-flight pass, 2026-09-15.** The auto-renew ruling §4 is explicit about its scope: the price line goes on *“all four `/kits/` routes”*, extended on 2026-09-11 to the three kit `/lp/` pages. It authorises **one sentence in a named place**. Row 32a is right that this overrides `site-funnel-model.md` §2 for those seven routes. 🔴 **But the ruling says nothing at all about a standalone public `/membership` explainer**, so that page is not covered by it — and row 32a, by treating both as one conflict, inherits the ruling's authority for a surface outside its scope. The page's own header does the same. This is precisely the **compressed citation** failure `03_compliance/STATE.md` names on 2026-08-27: *“a compressed citation keeps the authority of what it cites and loses the scope”*. The page is indexable and canonicalised when the flag is on. ⚠ **Row 32a's surface list also omits `/demo`**, which prices the membership in two places and which `site-funnel-model.md` §2 lists as an acquisition surface. **The question for Keith is not “which document wins” but “does a public priced membership page exist at all”** — and that question has never been put. | n/a, a ruling is missing | **Keith**, and it is a different question from row 32a's |
| 32e | `/demo` | ✅ **CLOSED 2026-09-16 BY CA-046 Q6 = A.** Ewa ruled the qualifier onto `/demo` and it is built. ⚠ **It went on in TWO forms, not one, and only the first is her approved sentence.** Where the membership list renders, the approved wording is carried **verbatim**: *"Clinician answers are general and published to every member; they are not individual medical advice and they do not replace your GP."* The benefit also appears as a **one-line row on the account screen** where that sentence does not fit, so it was shortened to *"Answered generally, published for all members. Not individual medical advice, and no replacement for your GP."* 🔴 **A SHORTENED QUALIFIER IS A NEW QUALIFIER**, so it claims no approval from Q6 and is asked as its own question in the CA-046 round 2 packet. Both verified in the rendered DOM, not in source. ⚠ **The underlying question row 32c left open is STILL open and Q6 did not touch it**: whether the benefit may be named on a public surface at all, given the live member screen has only ever rendered that block EMPTY. <br><br>~~⚠ **ROW 32c's PHASE-0 QUALIFIER WAS NEVER PROPAGATED TO THE OTHER SURFACE THAT RENDERS THE SAME BENEFIT.**~~ **The original finding, kept because it is what the fix was made against:** Independent pre-flight pass, 2026-09-15.** Row 32c records the added sentence — *“Clinician answers are general and published to every member; they are not individual medical advice and they do not replace your GP.”* — as *“the one finding the Guardrail #1 pre-flight produced”* against `/membership`. `/demo` renders the identical *“Ask the clinician”* benefit, in two places, to the identical cold visitor who has never tested, **with no qualifier at all**. Row 32c is scoped to `/membership` only, so nothing carried the fix across. 🔴 **This is the general shape of row 32c's own risk, not a new one:** the wording was signed for a member who has met the product and is being read by a stranger, and `site-funnel-model.md` §2 forbids an acquisition surface implying clinical services are live. ⚠ The underlying question row 32c left open is also still open and is unaffected by propagating the sentence: whether the benefit may be named on a public surface at all, given the live member screen has only ever rendered that block EMPTY. | **No new words** to write — the qualifier already exists and is approved; it is a placement fix. | **Ewa + Keith**, as part of the CA-046 question set |
| 33 | `/membership` | **NEW ASSET, 2026-09-08: `img-9.jpg`** (gpt_image_2, 4:3, resized to 1600x1194 to match `img-8`). A plain paper wall calendar in a domestic kitchen with one date ringed in blue biro. ⚠ **It goes on the CA-045 register as a genuinely new asset**, the second after `img-8`. **Why this subject:** the eight existing photographs are seven men in domestic settings plus a postbox, and CA-045 q3/q4 both ask whether an ordinary man beside tiredness copy implies he is unwell; a ninth man would inherit that question. This frame has no person, no hands, no clinic, no blood, no sample, no product and no lettering of any kind (digits only, verified in the render), so it opens no judgement the packet is already asking. New caption: **"A date, written down"**, following the existing convention (a place and a time, no claim, no product, no number). | **Yes, new**: one alt string and one four-word caption. No claim. | **CA-045 signers** (Ewa + Keith), and pre-flight |
| 34 | `/membership` | ⚠ **Registered because it widens an OPEN compliance question to a SEVENTH surface, no words involved.** The shared `HeroField` data layer renders in this hero too. CA-045 q6/q7 are open against exactly that layer ("hero data field: display or texture"), and row 18 last counted five surfaces, then six with `/demo`. The packet as drafted describes one. | **No words.** | **CA-045 signers**, as a scope note before the packet is sent |

| 35 | `/blog`, `/blog/[slug]`, `/blog/preview/[slug]`, `/authors/[slug]` | 🔴 **THE BLOG IS REBUILT IN DIRECTION F, 2026-09-08, AND THIS OVERTURNS A STANDING RULING.** On 2026-08-27 Keith ruled AGAINST the first F blog: *"I think the old or the live blog style wins. There's a lot of detail missing from the F blog you created, which at the moment I don't know if you can capture."* `blog-F.html` (2026-08-29) was drawn as the answer and its own header says it *"does NOT overturn the 2026-08-27 ruling on its own... that ruling stands until Keith rules again."* **Keith ruled again on 2026-09-08: "rebuild it with the new website... make sure that it sticks to the design of the new website in both look and feel."** That is the second ruling the mockup was waiting for. | **No article copy changed.** Not one word of any of the 18 MDX bodies, frontmatter, FAQ, or reference list was touched. This row is here because the CONTAINERS every approved claim sits in have all changed. | Pre-flight, placement check across all 18 articles |
| 35a | Blog, all surfaces | **What the rebuild is, in one line: the twelve-piece editorial component system was preserved and re-expressed, not flattened.** That was the whole of Keith's objection. `styles/components/f-blog.css` (new, `fb-` namespace) gives F an emphasis ladder deep enough to hold all twelve: four rungs on RULE WEIGHT (2px hairline Caveat, 3px Note, 5px PullQuote, 8px Punchline), then four on GROUND (`--core` + hairline StatBox, `--core` + ink ring SystemAlert, tray + `--core` inner ClinicalInsight, tray + `--sunk` inner InlineKitCTA), then the inverted EvidenceBox at the top. Verified in the browser by reading computed background and border off every rendered piece and asserting no two share both. | **No words.** | Pre-flight, as evidence the detail survived |
| 35b | Blog, all surfaces | **What came off:** `.blog-skin` and the cream `#f4f4f0` ground, the 8px black frames, the 4px rules, `bg-dot-pattern`, `brutal-shadow`, the uppercase black sans headlines, the `text-stroke` wireframe hover on the closing CTA, and `.article-prose`. `styles/base/blog-skin.css` is **deleted**: it was a written licence to break two brand non-negotiables (the global box-shadow ban and the white-only marketing ground) and neither exception is needed now the token release permits radius and the ambient shadow site-wide. The ~130-line `.article-prose` block was removed from `globals.css` in the same change, once its last consumer moved. **The blog stops being a documented special case.** | **No words.** | Pre-flight, as a note |
| 35c | `/blog/[slug]` | ⚠ **THE PHOTOGRAPHS, and they are the one thing the mockup does not draw.** 10 of the 18 articles carry an Unsplash hero (`photoSrc`/`photoAlt`/`photoCredit`/`photoCreditUrl`); `blog-F.html` draws the article, the twelve pieces, the tail, the index and the author page, and **no photograph anywhere**. So the treatment is DERIVED from `.f-shot` in f-primitives rather than invented: same grayscale, same radius, same caption furniture, same hover de-saturation as every photograph on `/`, `/kits` and `/how-it-works`. 🔴 **The Unsplash ToS attribution is preserved verbatim and may not be removed**: visible "Photo by X on Unsplash" plus UTM on both links. The grayscale is also the brand mitigation, since stock photography is in the brand DON'T column. **No new image assets were generated**; these are the same 10 photos already live, re-framed. | **No words changed.** The credit line is the same sentence in a new container. | Pre-flight, placement check; **no new CA-045 item** |
| 35d | `/blog` (index) | **ONE STRUCTURAL CHANGE, registered because it is not a restyle.** The V2.0 index was a full-bleed alternating left/right stack: one article per band, headline at 3xl to 5xl, sides flipping each row, roughly one and a half articles visible at 1440. It is now a card grid, one lead card spanning the row then three-up, so about twelve articles occupy the space two used to. The category filter, pagination, featured/lead distinction, empty state and the real-photo-vs-generated-OG-card grayscale rule all survive. **New copy, small:** the index hero standfirst (*"What your numbers actually mean, what a reference range is and is not, and what the evidence says about moving one. Written plainly, reviewed by a GMC-registered GP."*), an `{n} articles` chip, and the closing block (*"A number you have watched move."* plus its sub). ⚠ The closing copy restates the site's existing argument and makes no new claim, but it is new wording. | **Yes, new**, three short blocks on the index only. | Pre-flight |
| 35e | `/blog/[slug]` | **New copy, small, all furniture rather than argument:** the FAQ heading became *"Frequently asked."* under a *"Common questions"* label (matching the F section grammar); the article closing CTA became *"Find out where you actually stand."* over the existing `KIT_PRICE_RANGE` line; the newsletter block became *"The occasional plain-English read."* under *"Not ready to test?"*. ⚠ The V2.0 closing CTA said *"System Directive: Baseline Check"* and the newsletter said *"Health Intelligence Newsletter"*; both are retired as brutalist voice that no longer matches the site. **The kit price range, the 2-to-5-working-day turnaround and the UKAS line are unchanged and still render from their existing sources.** | **Yes, new**, three headings and two sub-lines. No claim in any of them. | Pre-flight |
| 35f | Blog, component labels | **Two literals retired, registered because they were customer-visible strings.** `SysHeading` printed a `SYS:` chip before every heading that used it, and `BlogToc` printed `SYS: On this page`. Both are gone: a machine prefix on a page that no longer speaks that way. `References` was `System DB // References` and is now `References`. ⚠ `SysHeading` is used by exactly 1 article and `NumberedHeading` by 1, so the blast radius is small. **`PublishedEvidence` is used by ZERO articles** and was rebuilt anyway, since deleting an unused approved component is a separate decision. | **Yes, three strings retired.** | Pre-flight |
| 36 | Blog | ⚠ **Three rendering defects were found and fixed during the rebuild, all three by rendering rather than by reading, and all three predate it or arrived with it.** (1) **Every in-article kit CTA button rendered its label invisible** (ink on ink) on 17 of 18 articles, because the prose link rule out-specified the button class. (2) **The references list rendered as a ragged grid**, because `display:flex` on a markdown-authored `<li>` turns every child into a column; that rule was inherited from the V2.0 code, where it had never been exercised. (3) **The page mounted TWO back-to-top buttons**, one from `ArticleLayout` and one built into `ArticleToc`, overlapping at different scroll thresholds. Also fixed: article dates rendered raw ISO (`2026-08-07`) on the index and author page while the article header formatted them (`7 Aug 2026`); there is now **one** `formatArticleDate` in `lib/blog.ts` that all three read. | **No copy.** (1) to (3) are defects; the date change makes two surfaces agree with the third. | Nothing owed; recorded so the pre-flight knows the surfaces moved |

| 37 | Every dark panel, site-wide | ⚠ **CONTRAST FIX, 2026-09-08, no words changed. Registered because it altered rendered TEXT COLOUR on a compliance-bearing block.** Keith reported the SystemAlert action bar as unreadable. Measured: **2.39:1 against a 4.5 requirement**, on the line that reads *"sudden or severe chest pain? call 999. don't wait."* Cause was specificity, not colour choice: `.fb-alert p` (0,1,1) beat `.fb-alert-act` (0,1,0), so a light-card body rule painted the safety line `--ink-2` on an ink ground. **A GP-referral instruction that a reader cannot read is a safety issue before it is a design one**, which is why this is on the register rather than only in STATE. A headless contrast audit across 16 routes then found 12 failing text nodes in 4 distinct places, all now at 0. **No copy was edited, added, moved or removed on any surface.** | **No words.** Rendered colour only. | Pre-flight, as a note; ⚠ **worth Ewa's eye** that the 999 line is now legible, since she signed the wording |

| 38 | `/about`, `/contact`, `/faq`, `/waitlist`, `/supplement-waitlist`, `/supplements`, `/supplements/daily-stack`, `/supplements/collagen` | 🔴 **THE EIGHT REMAINING CONTENT-MARKETING ROUTES ARE DIRECTION F, 2026-09-09.** Route conformance went 16/36 to 26/36 (44% to 72%). **No journey frame exists for any of them**, so layout was decided against DESIGN.md rather than ported from a drawing; every structural call is argued in each file's header. **What did NOT change: every claim, every range, every dose, every EFSA sentence, every FAQ answer, the CA-026 A1 standing claim on `/about` and the CA-026 C2 price answer on `/faq`.** All are byte-identical to the V2.0 pages. What DID change, and all of it is furniture: (a) **section labels**, because the F section grammar (`.f-blab` → `h2` → lede) is one label per section and the V2.0 pages had none; (b) **`/about`'s primary CTA**, "Choose your test" → **"See the tests"**, matching `/how-it-works` and `/membership` on the identical link to the identical destination, the same shape as row 28; (c) **`/supplements`, `/supplements/*`' eyebrow**, `Supplements // Launching Shortly` split into `.f-eyebrow` "Supplements" beside `.f-kchip` "Launching shortly", because the double slash is a V2.0 device and `.f-eyebrow` is one per page; (d) five short **close blocks** where the V2.0 page ended on a band with no ask. ⚠ Headline LINE BREAKS were re-placed on three heroes so no line orphans a word; not one word moved. | **No claim, range, dose or approved sentence changed.** New words are section labels and five close blocks, none carrying a claim. | Pre-flight, as a placement check across eight routes |
| 38a | `/supplement-waitlist` | 🔴 **A COPY DEFECT WAS FOUND AND DELIBERATELY NOT FIXED, BECAUSE FIXING IT IS A COPY CHANGE.** FAQ 4 answers *"Can I choose which product I want updates about?"* with *"Yes. The form lets you tell us whether you are interested in the Daily Stack, the Joint and Recovery Collagen, or both. You can change your mind later."* **The form on that page has no such control and never has.** `SupplementWaitlistForm` renders `interestedInProduct="any"` on this route and forwards it as a HIDDEN field; there is no product selector, and no route by which a reader could "change their mind later". It reads as true because `/supplements/daily-stack` and `/supplements/collagen` pass real values, so the FIELD exists in the payload while the CONTROL does not. Either the control gets built or the answer gets rewritten. **The answer is rendered unchanged pending that decision.** Logged as task-observer OBS-670. | n/a, an existing claim is wrong | **Keith**, then pre-flight |
| 38b | `/contact` | ⚠ **AN INTERACTION CHANGED: the FAQ is an open grid, not an accordion.** `FaqAccordion` is a V2.0 component with no F equivalent, and `/how-it-works`, `/membership` and every kit page render their questions open in `.f-faqgrid` (standardised by Keith on 2026-08-29). Four short answers behind four clicks, on a page whose own copy says *"worth checking before you write to us"*, is the accordion working against the sentence. **No answer text changed.** ⚠ Also removed: the five routing-card SVG icons and the Dr Ewa "Identity Verified" placeholder card. Direction F has no icon vocabulary (its one arrow is ruled to be a typographic glyph, "never a drawn SVG"), and a mono label reading "Identity Verified" over an outlined person glyph is a verification badge that verifies nothing, which is the device the 2026-09-03 ruling took off the footer chips. Dr Ewa is now `.f-initials`, as on `/kits/hormone-recovery`. | **No words changed** by either removal. | Pre-flight, as a note |
| 38c | `/faq` | ⚠ **THE STICKY JUMP NAV IS GONE; ITS SIX ANCHORS ARE NOT.** The V2.0 page pinned a second nav bar at `top-20` under the site nav, which is two fixed bars on a phone and a permanent bite out of the viewport on the longest page on the site. The same six links now sit once, in the hero, as `.f-kchip` anchors. **Every `id` is preserved**, so every inbound deep link still lands. ⚠ **And the page went from THREE full-bleed ink blocks to ONE.** DESIGN.md allows one inverted panel per page and reserves it for a conformity statement; `/faq` spends its one on **CA-026 C2, "Why do your tests cost more than a £45 test?"**, rendered verbatim. The NHS-gap section and the closing CTA lose their ink grounds and keep every word. 🔴 **The C2 answer is now a single constant read by BOTH the rendered block and the `FAQPage` JSON-LD**, which were two hand-maintained copies of one paragraph on the V2.0 page and could have drifted silently. | **No words changed.** | Pre-flight; ⚠ worth a look that C2 still reads as approved in an ink panel rather than a grey card |
| 39 | `/privacy`, `/terms` | 🔴 **THE TWO LEGAL DOCUMENTS ARE DIRECTION F, AND NOT ONE WORD CHANGED, MECHANICALLY ASSERTED.** Both routes are 28-line shells that inject 793 and 750 lines of V2.0 brutalist HTML from `canonical-site/*/index.html`. **Retyping them into JSX was rejected**: it puts a human transcription step between an approved legal document and what a customer reads, and nothing downstream would catch a dropped clause or a softened obligation. Instead `lib/legal/canonical.ts` rewrites **attributes only** and never touches a text node, and **`scripts/verify-legal-text.js` (new, in `npm test`) extracts every text node from the canonical source and from the transform's output and fails the build if they differ by one character.** Both guards were mutation-tested: break the transform on purpose, confirm red, restore. Current: privacy 265 text nodes identical, outline h1/h2/h3 = 1/13/8; terms 215 identical, 1/15/6. **What the documents lost: their ink panels, their eighteen SVG icons and their `border-b-4` frames.** Direction F allows one inverted panel per page for a conformity statement, has no icon vocabulary, and gives prose no container at all; a privacy policy is 793 lines of prose. Headings keep their levels and their order, so the outline, the anchors and screen-reader navigation are unchanged. | **NO WORDS CHANGED, and this is the one row on this register where that is asserted by a test rather than by care.** | Pre-flight, as a note only; **no re-approval owed** |
| 39a | `/privacy`, `/terms` | 🔴 **A SILENT-FAILURE PATH WAS REMOVED, AND IT IS A COMPLIANCE FIX RATHER THAN A RESTYLE.** Both route files ended with a fallback returning `<h1>Privacy Policy</h1>` (or Terms) above the sentence *"The canonical privacy content could not be loaded."* So a read failure or a changed anchor served a **200 with a real legal-page heading and no legal document under it**, and the only signal was one sentence a reader had to notice and act on. A privacy policy that silently is not there is a UK GDPR Article 13 failure wearing the costume of a working page; terms a customer is told they agreed to, absent from the page they were pointed at, is a consumer-law problem before it is a bug. `getLegalDocument` now **throws**, which surfaces as a build failure or a 500. ⚠ Also collapsed: the two routes used **different extraction regexes** for one job, and the terms one ran PAST `</main>` and swallowed everything up to the footer comment. One window now, for both. | **No words.** A failure mode changed. | Nothing owed; recorded because the failure it removes is a compliance one |
| 40 | `/lp/collagen`, `/lp/daily-stack` | ⚠ **A SHARED COMPONENT WAS ABOUT TO DRAG TWO UNREBUILT ROUTES INTO DIRECTION F, AND WAS STOPPED.** `SupplementWaitlistForm` has six call sites: the four supplement routes rebuilt above, and these two `/lp/*` pages, which are still V2.0 and are **blocked from rebuild behind the 2026-09-07 auto-renew ruling**. Restyling the component outright moved the seam rather than closing it: the conformance run immediately afterwards showed both `/lp/*` routes jumping from 0 F classes to 10, i.e. a Direction F card sitting inside a brutalist page nobody has reviewed. Fixed with a `variant` prop **defaulting to `'v2'`**, so both routes render byte-identical markup to what shipped, and the four F pages opt in. This is the ruled `RelatedArticles` fix from DESIGN.md gap 4 applied to a second component: *"the host declares its own world, which is the one thing the host reliably knows."* Re-measured after: both `/lp/*` back to 0. | **No words on any surface.** | Nothing owed; recorded so the `/lp/*` rebuild knows the variant exists |

| 41 | `/checkout/details`, `/order/confirmed` | 🔴 **THE BUY STAGE IS DIRECTION F, 2026-09-11.** Route conformance 26/36 to 28/36 (72% to 78%). Layout ported from **Frames V, V2, W and W2** (`buy-F.html`) rather than decided, unlike rows 38 to 40. 🔴 **THE CA-018 CONSENT SENTENCE IS REPRODUCED CHARACTER FOR CHARACTER**, including the sentence-final period outside the link. It is the Article 9(2)(a) health-data consent, approved by Ewa and Keith, pinned to `HEALTH_PROCESSING_CONSENT_VERSION` (`2026-06-23-v1`), captured at the point of purchase and gating payment; its own source comment says any wording change needs a new version string plus a fresh CA record. 🔴 **THE 18 GATE IS STILL ENFORCED TWICE AND STATED THREE TIMES**: the date input is still `type="date"` carrying `max` at today minus eighteen years, `isAtLeast18()` still re-checks on submit, and "18+ only" is still in the trust row. Frame V flags precisely this as what a restyle deletes without anyone noticing. **All six error strings, both button labels, both hints, the trailing note, both order-reference states, all three step bodies, both account-block headings and bodies, all four button labels and all eight trust items are byte-identical.** Not one branch changed: the first-failure validation order, the `discount` read, the bundle threading, the `/auth/post-checkout` redirect and its `post_checkout=1` loop guard are untouched. **What DID change, and none of it is a word:** (a) the sex control renders **"Male" / "Female"** where the V2.0 markup printed the raw stored values and uppercased them in CSS, so the control read "MALE" / "FEMALE" — the stored values are unchanged and no word differs, this is letter case only; (b) **`/order/confirmed`'s step 03 loses its ink panel**, because the direction spends one inverted panel per page on a conformity statement and that one was spent on the third of three equal steps, against the 2026-09-02 containment ruling that gives all four prose grids one treatment; (c) the checkout's trailing note is left-aligned where V2.0 centred it, matching Frame V2; (d) `/order/confirmed`'s "What happens next" becomes a `.f-blab` section label, which it already was as a heading. 🟢 **Two accessibility additions, neither touching copy or a branch:** the sex buttons gain `role="group"` with a label association and `aria-pressed`, where before a screen reader was told nothing about which was selected. | **No claim, range, dose or approved sentence changed. The one text difference is letter case on two form-control labels.** | Pre-flight, as a placement check on two routes; ⚠ **worth confirming the CA-018 sentence still reads as approved inside an F card**, since it moved container |
| 41a | `/lp/collagen`, `/lp/daily-stack` | ⚠ **ROW 40 OVERSTATES BOTH ITS COUNT AND ITS BLOCKER, CORRECTED HERE RATHER THAN EDITED SILENTLY.** Row 40 says `SupplementWaitlistForm` has **six** call sites; it has **eight** (`/supplement-waitlist`, `/supplements`, and two each on `/supplements/collagen` and `/supplements/daily-stack`, plus these two `/lp/*`). The count matters because row 40's whole lesson is "check a shared component's call sites before restyling it", and it is the number a future reader will trust instead of re-grepping. Row 40 also says these two routes are **"blocked from rebuild behind the 2026-09-07 auto-renew ruling"**. They are not. That ruling's section 4 names three routes explicitly — `lp/testosterone`, `lp/hormone-recovery`, `lp/energy-recovery` — and marks them NOT DECIDED with Keith as owner; it governs a price line under a **buy CTA**, and these two pages carry `SupplementWaitlistForm` and **no `CheckoutButton` at all**, so the ruling cannot reach them. The glob `/lp/*` is what recruited them. **Neither is rebuilt yet and neither is blocked**; the `variant` prop row 40 added is exactly what they need when someone picks them up. Logged as task-observer OBS-705. | **No words on any surface.** | Nothing owed; recorded so the `/lp/*` rebuild is not deferred behind a ruling that does not name these two |

| 42 | All five `/lp/*` routes | 🔴 **THE FIVE LANDING PAGES ARE DIRECTION F, 2026-09-11.** Conformance 28/36 to 33/36 (78% to 92%). No journey frame exists for any of them: Frame AC draws the `/lp` SHELL and a variance table and deliberately draws no page, because *"drawing five asserts they should stay five, drawing one asserts a template that does not exist"*. So layout was decided against DESIGN.md and argued in each file's header. **Every claim, dose, EFSA sentence, FAQ answer, price and attributed quotation is byte-identical.** What changed, and none of it is a claim: (a) **section labels**, the F grammar's one per section; (b) **the eyebrow split** on the two supplement pages, `Supplement // X // Launching Shortly` into `.f-eyebrow` beside a `.f-kchip`, the same change registered as row 38(c); (c) **the FAQ is an open grid, not `FaqAccordion`**, per Keith's 2026-08-29 standardisation and the `/contact` precedent; (d) **Dr Ewa's outlined-person "identity" glyph is gone** from `/lp/collagen` and the kit LPs, the `/contact` removal applied again: a verification badge that verifies nothing. She is `.f-initials`. 🔴 **THE SAMPLE REPORTS ARE NO LONGER TYPED ON THE LANDING PAGES, AND THAT IS A CLINICAL CORRECTNESS FIX.** Each kit LP hand-wrote its own copy of the same kit's readout, and DESIGN.md gap 9 records that the 2026-09-02 verdict-vocabulary sweep reached the `/kits/*` pages and never reached these: the scanner's first run found **twelve live instances** of the retired vocabulary here, including **free testosterone graded "Low" at 0.244 on `/lp/testosterone` and at 0.231 on `/lp/hormone-recovery`**, when the reference low is 0.198 and the engine returns `ft-normal`. All three readouts were extracted BY SCRIPT into `lib/kits/sampleReadout.ts` and verified token-for-token (194 data tokens identical), so both routes now render the swept data and a redesign has not re-typed a clinical verdict. ⚠ **Two live defects in the shell were fixed**: `<main>` carried `pt-20`, V2.0's flush-bar clearance, so content sat under the floating F nav; and the `.js` reveal gate was missing from the `/lp` tree entirely, so any `.f-rise` would have had nothing to reveal it. ⚠ **The third comparison table on `/lp/hormone-recovery` is kept and flagged**, not fixed: `/kits` and `/kits/hormone-recovery` carry two others of the same three products, and removing a section is a content decision. | **No claim, dose, EFSA sentence or approved answer changed.** New words are section labels only. | Pre-flight, as a placement check across five routes; ⚠ **the twelve corrected verdict words are worth Ewa's eye**, since they change what a marketing page says about a sample result |
| 42a | `/kits`, all three `/kits/*`, all three kit `/lp/*` | 🔴 **THIRTEEN SENTENCES TELL A BUYER THERE IS NO SUBSCRIPTION, AND THE AUTO-RENEW RULING MADE ALL THIRTEEN FALSE. NONE WAS REWRITTEN. OWED TO KEITH, THEN PRE-FLIGHT.** Keith ruled on 2026-09-11 that the three kit LPs carry the subscription price line, closing `2026-09-07-auto-renew-at-day-30.md` §4; building it surfaced that the DECIDED half had never been built (the line existed nowhere but a private constant inside `/membership`), and that the site contradicts it in thirteen places: `/kits` **"no subscription unless you choose one"**, the three `/kits/*` pages **"One-off purchase..."** twice each, `/lp/testosterone` **"One-off purchase. Includes lab fees & delivery. No subscription."** plus an FAQ answer, `/lp/energy-recovery` **"Secure checkout. No subscription."**, and `/lp/hormone-recovery` the same line plus **"One-off purchase."** plus an FAQ reading **"It is a one-off payment, not a subscription."** Under the adopted model nobody chooses a subscription: it arrives with the kit and charges on day 31. 🔴 **THE WORST IS CA-026 C1**, rendered verbatim in `/kits`' inverted panel, whose own source comment says *"rendered VERBATIM. The redraw changes only the container."* Changing it needs a fresh CA record. **All thirteen are rendered unchanged**, the same call `/supplement-waitlist` FAQ 4 got as row 38a. What ships instead is an interlock: **`scripts/verify-subscription-claims.js`, new, in `npm test`**, lists every claim on every run and FAILS whenever `MEMBERSHIP_ENABLED` is true while any remains, so the flag cannot be turned on until the copy is swept. ⚠ **This is one decision with row 12a, not two**: the homepage membership sentence is the same fact on a ninth surface. | **Yes, a rewrite of thirteen sentences.** | **Keith**, then pre-flight. ⚠ **BUSINESS, NOT CLINICAL** (Keith, 2026-09-11): this row first said the C1 re-record needed Ewa and it does not. What changes is payment terms, and the register records her CA-026 remit as *"clinical/principle"*; CA-021 is the precedent for a business-only approval. Ewa re-enters only if a draft reworks C1’s OTHER clause, *"the next step is a GP conversation, and we earn nothing from it"*, which is the conflict-free principle. Keep the two clauses structurally separate and she is not in the loop. |

| 43 | `/activate`, `/not-found`, `/subscription/confirmed`, and the new `/how-to-sample` | 🔴 **THE LAST THREE ROUTES ARE DIRECTION F, 2026-09-12. Conformance 33/36 to 36/36 (92% to 100%): every measurable route on the site is now Direction F.** The three were the remainder after batch 4 and none was a restyle. `/activate` was **deprecated on 2026-06-12** (`docs/2026-06-12-activate-qr-deprecation.md`, owner Keith) and is now a `redirect()` to the replacement that decision named, `/how-to-sample`, built here from **Frame AD** (`lp-sample-F.html`) — rows 44 and 44a. `/not-found` keeps every word and changes its skin (row 43a). `/subscription/confirmed` had no frame and was confirming a product that cannot be bought (row 45). `app/activate/layout.tsx` and `styles/pages/activate.css` are deleted with the page body, which leaves **one** V2.0 page stylesheet standing (`dashboard-panels.css`, held open by `DevFixtureBar`). The deprecated API, lib and components under `activate/` are left in place with their headers, per that decision's marked-not-deleted call. | **No words changed by this row itself.** See 43a, 44, 45. | Pre-flight, as the index for the three rows below |
| 43a | `/not-found` | ⚠ **THE 404 IS DIRECTION F AND EVERY WORD IS VERBATIM**, mechanically: label, headline, standfirst, both button labels, the "Or go straight to" label, all six routes back and the support line are byte-identical to the page built on 2026-08-29. Frame AI is a record of that build rather than a proposal, so the rebuild made the two structural decisions the frame could not. **No tray**: Frame AI draws the whole page inside a card, which predates the containment ruling of 2026-09-02 — a card holds a transaction or an instrument, and an apology is neither. **The six routes back are `.f-xlinks` chips rather than an underlined column**, the shipped pattern for "a short row of places to go instead" (the auth card); same six destinations, same order, same words. Two live defects went with the restyle: `<main>` carried `pt-20`, V2.0's flush-bar clearance, under a floating nav that needs 92/104 (the same defect batch 3 found on the app layout), and the page carried no `.js` gate or reveal observer, because `RevealGate` lives in the two layouts and the 404 is in neither. | **No.** Skin and structure only. | Pre-flight, sanity check only |
| 44 | `/how-to-sample` (**new page**) | 🔴 **THE ONLY GENUINELY NEW PAGE IN THE REBUILD, AND MOST OF IT IS TRANSCRIBED.** Frame AD. The five steps are the `INSTRUCTIONS` array from the deprecated `/activate`, byte-identical — the part of that flow that survives its deprecation, while the login gate, the per-order kit code and all three error states go with the route. The "if the blood will not come" aside is that page's own callout, verbatim, in a `.f-well`. The turnaround sentence reads `SLA_COPY` from `lib/pricing.ts`. **The standfirst is the row's only new copy: two new sentences** ("Five steps, about five minutes. No account needed, and nothing to enter.") followed by **existing live copy carried from `/how-it-works`** ("Do it fasted, first thing in the morning, for the most accurate hormone results.", also on `/` and `/test-selector`). No claim, no price, no CTA beyond a ghost link to the dashboard. | **Yes: two new sentences.** The rest is existing copy in a new placement. | Pre-flight |
| 44a | `/how-to-sample` | 🔴 **THE HOW-TO-SAMPLE FILM IS STILL OWED, AND THE PAGE SHIPS WITHOUT MENTIONING IT.** The 2026-06-12 decision's replacement is "a short how-to-sample video plus the step text"; no such film exists. Frame AD draws an **empty slot captioned "does not exist yet"**, which is the honest way to draw a dependency in a mockup and the wrong thing to ship — a live page telling a man holding a lancet about a film he cannot watch is worse than a page that does not mention one. **Built without the slot**; when the film exists it goes between the standfirst and the steps. ⚠ Also a decision Keith can flip: the page is `noindex, follow` and deliberately absent from `app/sitemap.ts`, on the reading that it is a support page behind a QR on the kit insert rather than an acquisition page. Making it indexable is a content decision (a thin page competing with `/how-it-works`), not a build one. ⚠ And the QR itself is unbuilt: the decision puts it **on the kit insert, not the sleeve**, and nothing prints one today. | **No new words in this row.** | **Keith** (the film, the QR, and the noindex call), then pre-flight |
| 45 | `/subscription/confirmed` | 🔴 **THE PAGE WAS CONFIRMING A PRODUCT THAT CANNOT BE BOUGHT, AND IT IS REWRITTEN RATHER THAN RESTYLED.** It read *"Your stack is starting"*, *"First box dispatching this week"*, *"First box ships … Free UK delivery. Letterbox-friendly"*, *"Auto-renews monthly … same delivery"* and *"UK manufactured"*. All three supplement subscriptions are `purchasable: false` in `lib/subscriptions/products.ts` — retired, kept only so an existing holder's row renders a name, and structurally impossible to check out. **The only purchasable subscription is the membership, which has no physical goods** (Keith, 2026-08-26), so the single success page of `app/api/checkout/subscription/route.ts` was promising a letterbox-friendly box to the only customer who can reach it. **This page carried no register row and no CA record**, so this is a correctness fix to unregistered copy, not a rewrite of approved copy. The replacement is assembled from approved strings wherever one exists: the renewal sentence and the cancellation sentence are `/membership`'s (*"That is an automatic renewal and we are not going to describe it as anything else"*, *"Cancelling is self-serve in your account and takes no longer than joining did"*), the retest heading is `/membership`'s §03 heading, the retest terms and the three includes are the in-app paywall's, and the clinician qualifier is the one the Guardrail #1 pre-flight added at row 32c. The price is read from `PRODUCT_MAP`, never typed. **New sentences: the standfirst, the eyebrow, the retest-date fallback line, and the whole "nothing to confirm" state** (a heading, a standfirst and a button label) for a visitor who reaches the page without a subscription. ⚠ **Not a live mis-statement today**: `MEMBERSHIP_ENABLED` is off in production (checked 2026-09-12) and the flag gates the checkout POST on the server, so no subscription can currently be started; the branch deploys nothing. It becomes live the moment the flag goes on. | **Yes, a rewrite**, with most clauses carried from approved copy. | Pre-flight. **Keith** on the new sentences; no clinical claim is involved, so not Ewa |
| 45b | `/subscription/confirmed` | **RE-CHECKED 2026-09-13. Three corrections to row 45, none of which changes what is owed.** ✅ **The deterministic pre-flight is CLEAN** on the rebuilt page: 0 HARD, 0 REVIEW, 3 CODE-COMMENT hits all in source comments. The CONTEXT.md judgement pass and Keith's approval of the four new sentences are still owed, so this advances the gate rather than clearing it. 🔴 **THE REBUILD IS NOT LIVE. `main` still carries the old page**, verified by `git show main:` and by fetching production: the live page renders *"First box dispatching this week"*, *"First box ships"*, *"Letterbox-friendly"* and *"UK manufactured"* today. Row 45 reads as though the page were fixed; it is fixed **on `redesign/direction-f`, which deploys nothing**, and this is the same branch-versus-production gap that ruling B's footer fix sat in for eleven days. ⚠ **Row 45's "not a live mis-statement today" reasoning is NARROWER THAN THE SITUATION, and the conclusion still holds for a different reason.** It argues from the flag gating the checkout POST, so nobody can *arrive* at the page. True, and the page is a **public GET route that returns 200 to anyone who types the URL**. What actually protects it is three things, not one: the flag, plus `<meta name="robots" content="noindex, nofollow">` on the live page, plus its absence from `app/sitemap.ts` and from any inbound link. Re-checked all three today. **A protection stated as one mechanism when it rests on three is a protection nobody can maintain**, because removing the noindex would read as an SEO tidy-up rather than as unshipping a guard. ⚠ Note `/membership` does not exist on `main` at all, so it is not a second instance: an anonymous request to it lands on the app host's login wall (defect H2), which is why an HTTP 200 there is NOT evidence about the flag. | **No new words in this row.** | **Keith**, unchanged: the four new sentences. Plus the merge, which is what makes the rebuild real |
| 45a | `/membership`, `/account/membership`, `/subscription/confirmed` | ⚠ **THREE COPIES OF THE MEMBERSHIP BENEFIT LIST COLLAPSED TO ONE SOURCE, NO WORDS CHANGED.** The three "what membership includes" sentences and the retest-terms sentence lived in the in-app paywall, were copied into the public `/membership` page under a comment reading *"CARRIED VERBATIM from the in-app paywall"*, and this batch needed them a third time. They are now `lib/membership/includes.ts`, read by all three, in the shape `MEMBERSHIP_DISCLOSURE` already set. A comment asserting two copies agree is an unenforced intention, and a benefit list that says different things on the page that sells the membership and the page that confirms it is exactly the divergence nobody would catch. The "three items, not four" argument (member price on kits came off 2026-08-26) moved with the strings. | **No.** One export, three consumers, identical rendering. | Pre-flight, placement check only |

| 46 | `/go` (the Instagram link-in-bio grid) | 🔴 **A CUSTOMER-FACING PAGE WAS EXCLUDED FROM THE REBUILD BY A WRONG DESCRIPTION, AND STAYED ON V2.0 FOR ALL OF IT.** `route-conformance.md` excluded `/go` as *"internal redirect, no UI"*. That describes `/go/[slug]`, a `route.ts` that records a click and redirects; `/go` itself is the rendered page every Instagram profile visitor lands on, with a headline, a standfirst, a tile per post in the 30-day carousel run and the conflict-free closing statement. Rebuilt in Direction F 2026-09-12 and the exclusion reason corrected in the generator. **Every word is unchanged with one substitution**: the turnaround was the literal "2 to 5 working days" typed into the file and now reads `SLA_COPY` from `lib/pricing.ts`, the same value `/faq` and `/waitlist` already import, whose comment carries the Ben/Vitall written confirmation of 2026-05-22. The UKAS ISO 15189 claim, the conflict-free footer sentence and the three close labels are untouched. ⚠ It stays out of the conformance sweep for a reason that is now true rather than wrong: the page fires `bio_grid_view` server-side on every render, so sweeping it would seed the campaign's own baseline with sweep traffic. | **No.** One literal replaced by the constant that already holds it. | Pre-flight, sanity check only |
| 46a | `/admin/dashboard`, `/ops/content`, `/blog/preview/[slug]` | ⚠ **THE THREE INTERNAL SURFACES ARE DIRECTION F, AND NO CUSTOMER-FACING WORD IS INVOLVED.** Registered anyway, because two of the three carry text a person outside the business reads: the blog preview is the surface **Ewa** reviews an article on, and its top bar is what tells her the draft is not live. That bar's two states keep their exact words (`PREVIEW · status: … · not public · not indexed` and `PROPOSED RE-OPT · revision … · NOT live · not indexed`), moved off six inline styles onto the shared internal strip. The admin and ops boards are behind a hard-coded admin allowlist, every number and label on them is carried verbatim, and their own prose (the claim-ledger and coverage notes) is reproduced word for word because each paragraph states a rule the numbers would be misread without. 🔵 **One question for Keith, and it is a design ruling rather than copy:** the ops board keeps its red/amber/green tones, which the 2026-09-03 ruling reserves for a clinical verdict. They are now three variables of their own, deliberately NOT `--color-status-*`, on a surface no customer can reach, and every tone is paired with a word. If the ruling is meant to hold everywhere, the change is three values in `f-internal.css`. | **No.** | **Keith**, on the tone question only |

| 47 | `/blog` + all 22 articles, all three `/kits/*`, `/results-dashboard`, every route (skip link) | ⚠ **FIVE COMPONENTS WERE STILL V2.0 ON ROUTES THIS REPORT SCORES AS FINISHED, AND FIVE LETTER CASES CHANGE. NO OTHER WORD DOES.** Defect register C1. `route-conformance.md` counts the `f-` classes a ROUTE renders, which cannot see inside a component, so `BundleChoice` (41 V2.0 tokens, 0 F classes, on all three kit detail pages and on the money path), `PasswordBanner` (32, opens the results dashboard by default), `NewsletterForm` (27, the blog's only email capture) and `SkipToContent` (4, site-wide chrome) all scored full marks while untouched. All four are rebuilt on the F component layer. **Every sentence is byte-identical**: the consent sentence, the mechanic paragraph, the savings note, both bundle CTAs, the prompt sentence, both validation messages and the success sentence. **What changes is case on five control labels** — `ENTER EMAIL ADDRESS` → `Enter email address`, `SET PASSWORD` → `Set password`, `SAVE PASSWORD` → `Save password`, `PASSWORD` → `Password`, `CONFIRM PASSWORD` → `Confirm password` — because the capitals were V2.0 STYLING typed into strings (each element also carried `uppercase` in its class list, so the shouting was said twice), and under Direction F a `.f-btn` label is sentence case while `.f-blab` uppercases in CSS, so the two field labels still RENDER in capitals. **Row 41 is the precedent, and it is the same call**: *"the only text difference anywhere is letter case on two form-control labels."* The arrow also moves out of the `SAVE PASSWORD  →` string into `.f-pip`, where every other arrow in the direction lives. 🔵 **One behaviour change, flagged rather than folded in: `PasswordBanner`'s modal is now an in-place disclosure.** The V2.0 overlay had no `role="dialog"`, no `aria-modal`, no focus trap, no Escape handler and no scroll lock; porting it meant porting four accessibility defects, and building it properly meant adding the system's first real modal for three fields. Same card, same three controls, `aria-expanded` + `aria-controls`. ⚠ **Not fixed, found on the way in:** `setPasswordAction` sets the dismissal cookie and revalidates, so on success the server stops rendering this component and the confirmation is unmounted with it — probably before anybody reads it. True of the V2.0 toast too. That is a server-action question, not a styling one. | **Case only, on five control labels.** No sentence, claim, price or approved block changed. | Pre-flight, case check only. Code `5dffd0b`. |
| 47a | `/error` (marketing), `/error` (signed-in app), `/global-error` | ⚠ **THE THREE ERROR BOUNDARIES WERE NEVER REBUILT, BECAUSE AN ERROR BOUNDARY HAS NO ROUTE.** Defect register C2, and the reason is the finding: every sweep of this rebuild worked from a route list (`route-conformance` walks 37 routes, `audit-dark-contrast` walks 16 URLs, the batch plans were written per page), so a file no URL resolves to was never in scope for any of them. `not-found.tsx` was rebuilt precisely because a bad URL reaches it. **Every sentence on all three is byte-identical**: the label, both headlines, both apologies, all four button labels and both support lines including the digest sentence. **Two additions on `global-error` only, and neither is a proposition:** the document now declares `lang="en"`, which it never did, and the card carries the words **"Andro Prime"** as a mark, because the page it replaces was an unstyled `<h2>` on browser-default white that gave a customer no indication of whose site had just failed. A company name on its own error page is a mark rather than copy. **No reference number was added there**, deliberately: the other two boundaries pair the digest with *"Email support@andro-prime.com and quote reference X"*, and putting a support address on `global-error` would be new customer-facing copy on an un-pre-flighted surface, so a bare reference would be a number with nowhere to go. ⚠ **C2's dark-mode reasoning does not hold and the row is still right.** It says the `bg-black` button is "a black button on near-black paper" in dark mode; `prefers-color-scheme` appears twice in the whole stylesheet set, both scoped to `.ap-themed`, which only `/demo`'s phone stage wears. What was true is that these were V2.0 on a site that is not, on the screen a customer meets at the worst possible moment. | **No.** Two additions on `global-error`, neither a claim: a `lang` attribute and the company name as a mark. | Pre-flight, sanity check only. Code `5dffd0b`. |
| 48 | `/` (homepage), `/supplement-waitlist`, `/go`, both `/authors/*`, all five `/auth/*` | ⚠ **WHAT A BROWSER TAB AND A SEARCH RESULT SAY, AND IT IS THE FIRST TIME ANY ROW HAS BEEN ABOUT THE `<head>`.** Defect register M1, M2 and M7. **Five pages typed "Andro Prime" into their own title while `app/layout.tsx` appends it with `title.template`**, so the tab read the brand twice: `/order/confirmed`, `/checkout/details`, `/how-to-sample`, `/subscription/confirmed` and `/go`, which rendered as **"Andro Prime | Andro Prime" and nothing else** on the page every Instagram profile visitor lands on. 🔴 **M1 named FOUR of those five. `/subscription/confirmed` was found by the check, not by the sweep that raised the row** — it sits behind `MEMBERSHIP_ENABLED` and 404s with the flag off, so a rendered sweep of 37 routes could not see it. Four titles simply lose the suffix, which removes words rather than adding any. **`/go` takes its own H1, "Every post, and where it goes"** — existing copy on that page, no new sentence. **The five `/auth/*` routes had no metadata at all** and rendered the root default, so login, reset and sign-up were three identically-labelled tabs at the one moment a man has all three open; each now carries **the heading its own card already renders** (`Access Your Results`, `Create Your Account`, `Reset Your Password`, `Get a Sign-In Link`, `A couple of quick questions`) plus `robots: { index: false }`. **M2's text lists four of those five and then says "these five pages"; the fifth, `/auth/link`, was done with them.** ⚠ **Two meta descriptions were SHORTENED, which is the only part of this row where words leave the product.** The homepage ran to 213 characters and was cut mid-sentence in search; it is now 156, assembled from fragments that are all verbatim (the opening and the GP-routing sentence from the old description, "UKAS ISO 15189-accredited lab, plain English" from this page's own `twitter.description`), and **what it loses is "One price, nothing hidden.", which is CA-026 C1 and is rendered on the page itself**. `/supplement-waitlist` was 161, one over, and "as soon as" became "once". ⚠ **The two author pages get a new `metaDescription` field rather than a shortened `bio`**, because `bio` is also the schema.org `Person.description`, which has no length limit and should not be cut to fit a snippet; every word of both is verbatim from the bio, truncated at a sentence boundary, and **Ewa's keeps the GMC line and her credentials in full** — what it drops is the description of her review duties. Held shut by **`scripts/verify-metadata.js`, new, in `npm test`** (the brand is never typed where the template appends it; an indexable title stays under 60 and a description under 160) and by **`scripts/audit-rendered-markup.js`, new, in `test:design:live`**, which reads the rendered `<head>`. ⚠ **That last clause read "the only thing that can measure the two routes whose metadata is computed from the database", and it was superseded four hours later by row 48b.** True when written, and the correction is the point of that row: a route sweep renders ONE sampled article, so it could only ever speak for the template. Article head lengths now belong to `npm run verify:article-seo`, whose unit is the article; `/authors/[slug]` is still measured here. | **Yes: five titles shortened, five titles added from existing headings, two descriptions shortened, two author snippets truncated.** No new sentence anywhere; every string is either existing copy or a cut of one. | Pre-flight. **Keith** on the homepage description losing the C1 price line, which is the only substantive cut. Not Ewa: no clinical claim changes, and the truncation of her bio removes duties rather than credentials. |
| 48a | Eight routes, `/kits`, and the site footer | ⚠ **HEADING LEVELS AND TWO IDs. NO WORD CHANGES, AND IT IS REGISTERED ANYWAY BECAUSE IT CHANGES WHAT SOME READERS HEAR.** Defect register M4, M5 and M6. A heading is markup, so by this file's own rule this belongs under "what does not": pure structure, words untouched. It is here because the outline a screen reader reads aloud IS a rendering of the copy, and this row **removes six items from it** (three `/faq` statistic captions, two price-row kit names, one per instance) and **promotes eight others**. **M6 measured eight routes skipping a level under a STATE figure that said "heading skips to 0"** — true of the routes measured in August, and every route rebuilt since was free to reintroduce it, because the number was recorded and the check was not. Three of the eight were the shared footer: its column headings went h4 → h3 in August with the reason *"the page's last heading is an h2"*, which was **a premise about every other page, held by nothing**, and three routes have no h2 at all. They are now h2, which can never skip from any page. Blog and author card titles go h3 → h2 (they are the first subject under the page h1); `/order/confirmed`'s three step titles likewise; `/faq`'s statistic captions and the `/kits` and `/waitlist` price-row kit names **stop being headings at all**, which is M6's own recommended shape for them, and `/kits` was changed alongside `/waitlist` although it did not fail, so the same row does not mean two things one click apart. Type is byte-identical on all seven surfaces, verified by screenshot rather than by reading the selectors. **M4**: `/waitlist` rendered its form twice with one hard-coded `id`, so both labels resolved to the first input and clicking the second form's label moved focus elsewhere on the page; `useId` now. 🟢 **M5 needed no work: it was already fixed four days before it was raised.** The blog thumbnails have carried `tabIndex={-1}` and `aria-hidden="true"` since `583ffc8` on 2026-09-08, and the row was written on 2026-09-12 — re-tested here, and the check was still built and proved, because the row's real content is that the defect class must stay shut. | **No.** Structure only. | Pre-flight, sanity check only. Code this session. |
| 48b | All 19 blog articles | 🔴 **KEITH'S DECISION, 2026-09-14: articles get an SEO field of their own, and THE CORPUS TURNS OUT TO BE SEVENTEEN ARTICLES RATHER THAN THE TWO ROW 48 NAMED.** Register M7's remainder. `seoTitle` and `seoDescription` are new optional frontmatter keys; `generateMetadata` renders them and falls back to `title`/`excerpt` when they are absent, so **an article that sets neither renders exactly what it rendered before.** ⚠ **The number is the reason this row exists.** The rendered route sweep reported an 89-character title and a 192-character description on the ONE article it samples, and both were correct — and the 5th and 7th worst of nineteen. Measured against the live corpus: **17 of 19 articles are over on at least one field, the worst description 260 characters and the worst rendered title 94.** A route sweep's unit is a URL and a dynamic route collapses a whole corpus into one, so its silence about the other eighteen was structural rather than reassuring. **NO ARTICLE COPY HAS CHANGED AND NONE IS PROPOSED HERE.** What shipped is the mechanism plus `scripts/verify-article-seo.ts`, which measures every article through the same resolver the page renders with, on a dated ratchet seeded with all 17 — a number that may fall and never rise, and that fails the check if an entry goes clean and is not removed. ⚠ **The 34 sentences are a COMMISSION, not a build task.** ⚠ **Two clauses of this row were superseded the same day and row 48c carries both. The count is 29, not 34** — 15 titles and 14 descriptions across the 17 articles, since five are over on one field only; 34 was 17 × 2. **And Ewa is no longer the route.** The original text follows as written. **** They are customer-facing copy on clinical articles and the descriptions summarise clinical content, so each takes the article route: drafted, Guardrail #1 pre-flight, and **Ewa wherever a shortened description moves a claim rather than just trimming one**. Two articles are already clean (`crp-blood-test`, `low-vitamin-d-symptoms`) and need nothing. ⚠ **The share card and the schema deliberately keep the editorial copy**: `openGraph`, `twitter` and the Article schema's `headline`/`description` still read `title`/`excerpt`, because a social card has no 60/160 truncation and the headline is what a reader recognises — the same split `lib/authors.ts` makes between `bio` and `metaDescription`, and a test asserts it so "tidying" it onto the SEO fields fails loudly. ⚠ **Keith asked for COLUMNS and these are frontmatter keys** — an engineering call inside his decision, flagged rather than folded in, and 🟢 **he ruled for the keys the same day, so this is settled rather than pending** (row 48c carries the closure): `blog_article_revisions` mirrors `body`, `frontmatter` and `keyword_coverage` and nothing else, so a column on `blog_articles` would have no home on a revision and **Ewa's preview — which renders a revision — could never show a proposed SEO description or let her rule on one.** The whole pipeline also passes the frontmatter blob through wholesale, so a key costs no pipeline code while a column needs a mapping at each end. | **No words changed.** A mechanism, plus a counted list of 34 sentences owed. | **Keith**, to commission the copy. **Ewa** per article, only where a shortened description moves a claim. Not a pre-flight item until copy exists. |
| 48c | The review ROUTE for all 19 blog articles, not their copy | 🔴 **KEITH'S SECOND RULING OF 2026-09-14, AND IT SUPERSEDES TWO CLAUSES OF ROW 48b: "review a revision of this type as it is an SEO description and tag, not a clinical observation."** Register M7. An SEO snippet no longer takes the clinical route. ⚠ **The count in row 48b is wrong and this row carries the right one: 29 fields, not 34** — 15 `seoTitle` and 14 `seoDescription` across the 17 articles, because five of the seventeen are over on one field only. 34 was 17 × 2, typed into four documents on the day the list was written; `verify:article-seo` now PRINTS both counts so the number is read rather than multiplied. ⚠ **The ruling needed a column, not a sentence.** Nothing in the pipeline reads a document, and `reopt-concierge` opened a task on the clinical list for every staged revision without exception — so a change to a `<title>` tag reached a GMC-registered reviewer under a checklist about EFSA claims, TRT boundaries and ashwagandha, none of which it could have touched. `blog_article_revisions` now carries a **`scope`** (`content` or `seo`, 169 existing rows backfilled to `content`); `scope='seo'` routes to Keith on **Approvals & Sign-offs**, logged with Keith as reviewer and **no GMC number**. 🔴 **The label is enforced in the DATABASE rather than asserted by the caller**, because a caller claiming "metadata-only" is making precisely the claim that lets it skip clinical review. `stage_blog_revision` raises if the body differs from the live body by a byte, or if the frontmatter differs anywhere outside the two SEO keys. Proved against the live database: body change refused, tampered `title` key refused, SEO-only change accepted, inside a transaction that rolled back. ⚠ **The two rulings COMPOSE, and the seam is mechanical.** Ewa's tier ladder (2026-08-18, Q14) calls a claim *"compressed, or on a surface that cannot carry the qualifier"* tier 2 and itemises it to her — which describes a 260-character description cut to 160 almost word for word. `scripts/content-engine/seo-revision-guard.ts` reads each proposed field against the article's own approved title, excerpt and body: a **rewording** is tier 1 and auto-passes with no clinician, which is Keith's ruling operating normally; a **net-new figure, a net-new citation, a net-new prevalence claim, or a dropped qualifier** falls through to Ewa carrying its findings, and her task says why it reached her. It fails toward review in every uncertain case. 🟢 **AND THE COLUMNS QUESTION IS CLOSED THE SAME DAY: FRONTMATTER KEYS.** Row 48b flagged that Keith asked for `seo_title` / `seo_description` COLUMNS and got `seoTitle` / `seoDescription` keys. He has ruled for the keys, so the flag is settled rather than pending. Three reasons are now recorded in `CONTEXT.md`, `STATE.md` and `lib/blog.ts` so nobody re-opens it on noticing the spelling differs from the ask: a column has no home on a REVISION and Ewa reviews a revision, so she could never rule on a proposed value; the pipeline passes the frontmatter blob through wholesale so a key costs no pipeline code; and the scope guard above compares `frontmatter - 'seoTitle' - 'seoDescription'` in ONE jsonb expression, which with columns would be a hand-maintained list of every other column. ⚠ **They must not be tidied into columns later.** | **No words changed, and none is proposed here.** A routing change plus the corrected count of what is owed. | **Keith** — this row RECORDS his ruling rather than asking for one. **Ewa** only on the fall-through, per article, and only where the guard finds a claim moved. Drafting and Guardrail #1 pre-flight are unchanged. |
| 48d | 15 of the 19 blog articles | 🟢 **KEITH APPROVED THE COMMISSION, 2026-09-14, AND IT IS THE FIRST COPY IN THIS REGISTER THAT NEVER GOES NEAR THE CLINICAL REVIEWER.** 26 fields, 15 published articles, three batches, drafted and applied. Board record ClickUp `869f1wwch`; repo mirror `03_compliance/content-approval/approval-record-m7-seo-fields-2026-09-14.md`. ⚠ **ONE WORD IN 26 IS NOT A CUT OF APPROVED COPY**: `fbc-blood-test` says "Shows" where the approved title said "Tells You", because 49 bare against a 46 budget and dropping " You" alone leaves a non-sentence. That verb is Ewa's own, in signed claim 39 (CA-042, 2026-08-18): *"Ferritin shows iron stores"*. Everything else is a deletion. ⚠ **Six descriptions drop the "Reviewed by GMC-registered GP Dr Ewa Lindo" sentence**, which is the only substantive loss and was put to Keith as a corpus-level decision rather than six inherited ones. It survives on five other surfaces (standfirst, byline, `og:description`, `twitter:description`, JSON-LD `description` + `reviewedBy`) because those read `frontmatter.excerpt` directly — verified in `page.tsx`, not taken from a note — and most of it was already past the 160 cut on the live snippet, so what it replaces is a clinician's name truncated mid-surname. Two further Keith decisions settled the same way: two predicate-free titles (`Inflammatory Markers Blood Test`, `Normal Testosterone Levels by Age`) and `how-to-read-blood-test-results` dropping "for Men". ⚠ **The one REVIEW hit is disclosed rather than buried**: `brain-fog` carries "fixable inputs", which trips the retest rule, is verbatim from the approved excerpt, and is ALREADY the live search snippet — so this row changes its exposure by not one character. No adjudication of the term exists in `03_compliance/`; if it is to be ruled on it belongs in an article-level sweep, not here. 🔴 **APPLIED AND NOT YET VISIBLE, WHICH IS THE THING TO READ TWICE.** The fields are on the live database and verified there; `resolveArticleSeo()` exists only on `redesign/direction-f`, 158 commits ahead of `main`, so production ignores them harmlessly. All 15 were revalidated (200 each) and the rendered `<head>` re-read afterwards — still the old values, which is expected and is why it was checked rather than assumed. **Nothing further is owed to make them appear; they surface with the branch.** ⚠ Three independent compliance reviews ran, one per batch, each adversarial. They found one real error (batch 1 originally included `cortisol-belly`, an unpublished draft with Ewa) and five defects in the checking tooling, all fixed and mutation-verified. | **26 fields written; one word substituted; six credential sentences and a handful of scope clauses dropped. No article body changed.** | **Keith, given 2026-09-14** — this row RECORDS the approval. The board flip to `approved` is his and is the one action outstanding, because that list admits only a named human. **Not Ewa**: all 15 returned `route: seo`, 0 findings, under her own tier-1 auto-pass. |

---

---

## Closed

> **The final pre-flight RAN on 2026-09-15** (Phase 4 of `qa/direction-f-migration-audit.md`).
> This section is no longer `_None yet._`, which was the signal that the gate had never run at all.
>
> ⚠ **Read what this section is.** It records the half of the gate a machine can discharge: the
> deterministic floor, the delta against what is live on `main`, and the mechanical assertions each
> row makes about itself. **It closes no row on its own.** Sign-off is Ewa's (clinical/claims) or
> Keith's (business) per `03_compliance/CONTEXT.md`, and the pre-flight skill's invariant 7 forbids
> the lineage that drafted copy from clearing it. A row moves out of "Open items" when a signature
> lands, not when a scan passes.

### ⚠ THE ROW COUNT EVERY OTHER DOCUMENT QUOTES IS WRONG, AND THE STRUCTURE IS WHY

The plan, `09_website-app/STATE.md` and the audit all say **48 rows**. There are **75**. Until
2026-09-15 the `## Closed` heading sat *in the middle of the table*, immediately after row 37, and
every session since simply appended below it — so rows 38 to 48d, **27 rows**, fell outside the
count and outside the "read this table first" instruction that opens this file. The newest and
most consequential rows were the invisible ones: 42a (thirteen sentences made false by the
auto-renew ruling), 45 (a page rewritten because it confirmed a product that cannot be bought),
and 48b–48d (the SEO commission). The heading is now at the end, where appending cannot jump it.

**The generalisable half:** a section that collects finished work must be the LAST thing in a file
that grows by appending, or it silently becomes a fence that new work lands behind. Its emptiness
was read for weeks as "nothing has been cleared"; it also meant "27 rows are not being counted",
and nothing distinguished the two readings.

### What the deterministic floor established

Unit of scan, stated per the skill's step 2a: **the 123 customer-facing `.tsx` files changed on
`redesign/direction-f` vs `main`**, excluding `(internal)/`, `/admin/`, `/ops/`, `/api/`. The same
scanner was then run over `main`'s own versions of those files (102 of them exist there), because
an absolute finding count on a rebuild is meaningless and only the delta answers the question the
merge asks.

| | `main` (live today) | `redesign/direction-f` |
|---|---|---|
| 🔴 HARD | **18** | **3** |
| 🟠 REVIEW | 40 | 36 |
| 🟡 code-comment (non-blocking) | 12 | 108 |

🟢 **The merge is a net compliance improvement of fifteen HARD findings, and the mechanism is row
42's.** Sixteen of `main`'s eighteen HARD hits are **verdict-vocabulary defects**: sample panels
grading a marker with a word `lib/results/resultSeverity.ts` does not return — `Borderline`,
`Normal`, `Low`, `Suboptimal`, `Elevated` — four on `/kits/testosterone` (rows 22, 23) and twelve
across `/lp/energy-recovery` and `/lp/hormone-recovery` (row 42). **The branch has zero.** Row 42
claimed twelve and row 22/23 the rest; both counts are correct and the split across rows is why no
single row states the total.

🟢 **The two largest blocks of new copy on the branch are clean at the deterministic floor.**
`/membership` (row 32, "the largest single block of NEW COPY"), `/demo` (rows 29–31),
`/how-to-sample` (row 44), `/subscription/confirmed` (row 45) and `/account/membership` each return
**0 HARD and 0 REVIEW**. This is the result the skill says is invisible if only totals are reported,
so it is stated explicitly.

🟢 **Row 35's central claim is mechanically verified: no article copy changed.** `git diff main HEAD`
over `*.mdx` returns **zero files**. Not one word of any article body, frontmatter, FAQ or reference
list was touched by this branch.

🟢 **Ashwagandha: clean, swept by synonym rather than by token** (invariant 4, skill step 2c). Seven
forms — the name, `withania`, `somnifera`, `KSM-66`, `KSM66`, `winter cherry`, `indian ginseng` —
across `app/`, `components/`, `content/`, `lib/` and `public/`. **Zero hits in every form.**

🟢 **Every EFSA claim renders in its approved wording**, checked against the table in
`03_compliance/CONTEXT.md` rather than from memory: zinc/testosterone maintenance, D3/muscle
function, B12/energy-yielding metabolism and psychological function, vitamin C/collagen for
cartilage. No rephrasing, no extension, and no unapproved variant anywhere in the changed set. One
cosmetic inconsistency only: two surfaces say "Vitamin D" where the table says "Vitamin D3".

🟢 **The supplement disclaimer's qualifier is intact, and the finding that said otherwise was an
artefact.** A line-scoped diff reported *"support normal physiological function **where a deficiency
has been confirmed**"* as present on `main` and absent on the branch — a dropped hedge, which is the
most serious shape a copy delta can take. The sentence is byte-identical; the branch wraps it across
three lines of JSX. Recorded because the artefact is asymmetric — formatters wrap and rarely unwrap,
so this class always presents as *the new version lost something*. (Observation 825.)

### The three HARD findings, and what each actually is

| Where | String | Status |
|---|---|---|
| `app/(marketing)/faq/page.tsx:172` | "NHS will typically treat at this level" | **Already live on `main`.** Carried, not introduced. Subject is the NHS, not us. Never graded. → 🟠 Ewa |
| `app/(marketing)/faq/page.tsx:524` | "a diagnosable deficiency that warrants treatment" | **Already live on `main`.** Carried, not introduced. Describes the NHS threshold's purpose. → 🟠 Ewa |
| `app/(marketing)/page.tsx:475` | "the NICE guideline our GP follows **treats** 25 to 70 as an indeterminate zone" | **New on the branch**, and 🔵 **previously adjudicated**: `03_compliance/STATE.md` records it as the *regards as* sense about a numeric band, reading as a false positive, and explicitly states it was **not** self-cleared and is owed a judgement pass. This is that pass. |

⚠ **"Already live on `main`" is not "approved".** It means the merge does not introduce the
exposure; it does not mean anyone has ever graded it. Both FAQ lines have been reaching customers
while ungraded, and that is a finding about `main`, not about this branch.

### The REVIEW bucket

36 hits, and the great majority are the EFSA claim sentences themselves — the scanner flags
"contributes to…" for a human decision by design, and every one matched the approved wording above.
**Four need a judgement that this pass cannot make, and all four are already live on `main`:**

1. `/supplement-waitlist:192` — the heading **"A purchase or a deposit"**. The deposit mechanic was
   **shelved 2026-05-08** (`03_compliance/CONTEXT.md`), so a live page offers a thing that no longer
   exists. → **Keith** (business framing).
2. `/supplements/collagen:113` — *"Supports joint comfort and mobility. Works alongside collagen to
   support your recovery."* Collagen itself has **no approved EFSA claim**; the only approved
   collagen-adjacent claim belongs to vitamin C and is about cartilage. → **Ewa**.
3. `/lp/collagen:182` — *"By 50, you have lost up to 20% of the collagen that supports your joints,
   tendons, and skin."* A quantified physiological assertion. → **Ewa**.
4. `/supplements/daily-stack:98` — *"this is the dose most research supports for correction"*. Both
   a substantiation claim and an efficacy-adjacent word. → **Ewa**.

### What the pre-flight did NOT and could not clear

- ✅ **CA-045** (`869eur84c`) — **APPROVED 2026-09-15**, read from ClickUp, not inferred from the
  repo. Ewa cleared all nine items with no conditions on her direct written reply (`1: A` through
  `7: A`); Keith signed the same day. ~~CA-045's packet is a Gmail draft that has never been
  sent~~ — it was sent 19:46 UTC and answered 20:07. ⚠ **The q6/q7 caveat below SURVIVES the
  approval and should be checked, not assumed away:** that wording describes one surface while the
  `HeroField` layer now renders on seven (rows 18, 34). She ruled the field is texture rather than
  a data display, and she did **not** take the option pinning its opacity, so nothing obliges a
  re-ask if it is raised on any of those seven.
- 🟠 **CA-046** (`869exuphq`, `pending`) — still open, still a merge blocker, unchanged.
- **Row 6** — the FAI verdict contradiction between `PRODUCT.md` and the `BADGES` map. Independent of
  the redesign and **wrong on `main` today**. → Ewa.
- **Rows 12a and 42a** — the sentences the auto-renew ruling made false. → Keith, then pre-flight.
- **Row 32a** — `site-funnel-model.md` §2 forbids an acquisition surface pricing the membership;
  the 2026-09-07 ruling §4 requires exactly that price line. Neither document was amended. → Keith.
- **Rows marked "placement only"** still need the contextual look the note at the foot of this file
  describes: a CA-026 approval attaches to a claim *in a context*, and the scanner cannot see context.

---

### The independent second pass (pre-flight invariant 7), 2026-09-15

The deterministic floor above is the machine half. Invariant 7 forbids the lineage that drafted
copy from clearing it, so the `compliance-reviewer` agent ran the judgement ceiling independently
over the same 149-file changed set, with `03_compliance/CONTEXT.md`, `STATE.md`,
`clinical-governance-copy-corrections.md`, the auto-renew ruling and `site-funnel-model.md` loaded
first. **It found nine things the scanner cannot see, all of them on `/demo` and its `AppShell`,
which the deterministic pass returned as 0 HARD / 0 REVIEW.** That gap is the argument for the
second pass, and it is recorded here rather than summarised away.

Every claim below was re-verified against the source before being written down; two were found to
be overstated and are corrected in place rather than repeated.

**🔴 The two to read first.**

1. **`/demo`'s compare toggle turns a See-Your-GP result into "No action indicated".**
   `components/app-shell/AppShell.tsx:507` is `const gp = goesToGp(r) && !compare`, so flipping the
   rail's checkbox removes the GP referral block, the receipt line and the GP-summary button. The
   badge is replaced with *"Within the normal range"* (`:518-524`) and the action block with
   *"What happens now / No action indicated."* (`:619-623`). The default marker is testosterone
   **10.5 nmol/L**, and the fixture's own header calls it *"the single sharpest screen in the
   drawing — the report routes him out of the business and says so"*, recording
   `testosterone 10.5 -> equivocal-testosterone (GP)`. So a public page one click from the homepage
   can present a GP-referral result as a calm all-clear.
   ⚠ **Correction to the finding as first written:** it claimed *"nothing on screen says our engine
   disagrees"*. That is not right, and the distinction changes the grade. The toggle carries its own
   explanation — *"Strips out our action bands and leaves only the laboratory reference range. That
   is the view your supplier's own portal gives"* (`DemoStage.tsx:425-430`) — which states the
   mechanism plainly at the control. The residual risk is **reading order**, not absence of
   framing: the card itself carries no marker-level reminder, so a reader who scrolls past the
   control sees only the all-clear. That makes this a judgement for Ewa about sufficiency of
   framing, not an unambiguous breach. → **Ewa**, as part of CA-046 q1.

2. **The subscription interlock does not catch "Pay once." on the page that takes the money.**
   `app/(marketing)/kits/page.tsx:201` renders *"Choose your kit. Pay once. Kit dispatched the same
   working day."* `scripts/verify-subscription-claims.js` matches only `no subscription`,
   `one-off purchase`, `one-off payment` and `not a subscription` — **zero occurrences of "pay
   once"** — so the guard passes a green build over the plainest statement of the thing the
   auto-renew ruling made false. Three more uncounted instances sit on the same seven surfaces:
   `lp/energy-recovery:376` and `lp/hormone-recovery:536` (chip *"all-in, one-off"*), and
   `components/commerce/BundleChoice.tsx:116` (chip *"One-off test"*, rendered on all three kit
   detail pages).
   **This is the script's own stated failure mode turned on itself.** Its header argues that *"a
   scope drawn around the known instances can only ever confirm the search that produced it"* — and
   its PHRASE LIST has exactly the defect it fixed in its DIRECTORY SCOPE: both were derived from
   the thirteen sentences already found. Row 42a's count of thirteen is correct for its own phrase
   set (all thirteen reconcile to file and line); the surface is larger than the phrase set.
   → **Keith**, and it is a code change to the guard, not a copy change.

**🟠 Also flagged, all on `/demo` / `AppShell`, none previously registered.**

| | Finding | Signs |
|---|---|---|
| F1 | *"Your retest on 12 November is what says whether it moved, **and it did**."* (`:866-871`) — a closed retest outcome on a screen whose subject is a supplement adherence loop. `CONTEXT.md` retest framing forbids language implying a supplement changed anything. The fixture applies that reasoning rigorously to testosterone and not to vitamin D, where the rise is drawn, charted and narrated. | Ewa + Keith |
| F2 | *"Andro Prime clinical logic · signed by Dr Ewa Lindo"* (`:615`) — a fourth attribution variant; `clinical-governance-copy-corrections.md` (APPROVED 2026-05-22) fixes the permitted phrasings and reserves the verb to her. | Ewa |
| F3 | *"A result that goes to a GP earns us nothing."* (`:631-633`) — its source comment cites register row 30 as already-signed wording; row 30 says the opposite, and no surface renders that sentence. The approved form is *"That result earns us nothing."* | Keith |
| F4 | A statutory right cited as the reason a product surface stays open (`:886-891`). Substance agrees with CONTEXT.md; the *"so"* makes a product decision read as legally compelled. | Keith |
| F5 | *"The other providers all draw one"* (`:587`) and *"what the other funnels put in front of the same number"* (`DemoStage:427-430`) — universal comparative claims with no substantiation on file. | Keith |
| F6 | The non-dismissible notice promises *"the same laboratory ranges, the same action bands, and the same explanations"*; compare mode strips the action bands and substitutes the explanation. Row 29 named that sentence load-bearing — *"if the demo ever stops rendering through the real classifier, this line becomes false"* — and a toggle does exactly that. | Ewa + Keith, CA-046 q1 |
| F7 | *"Out of range"*, *"Within the normal range"*, *"Outside the reference range"* render as toned verdicts and are not in `BADGES`. The file clears itself in a code comment (`:149-156`); a signed exception needs a real CA number and there is none. | Ewa |
| F8 | `/lp/hormone-recovery:159-162` badges *Optimal / Suboptimal / Review / Complete* and `/lp/testosterone:146` says *"Borderline or suboptimal"*. Carried from `main` with a letter-case change only. `Optimal` is an engine word but Keith's 2026-08-07 ruling reserves it to testosterone alone; here it badges all nine markers. | Ewa |
| F9 | `/membership:498` states the 90-day flagged-marker retest cadence to a cold buyer. Row 32 flags the intervals for Ewa but does not connect them to **CA-026 audit item F4**, which `03_compliance/STATE.md` records as still open on the confirmatory-testing boundary. | Ewa |

**⚠ Six corrections owed to this register itself**, found by the independent pass:

1. **Row 30's "three prototype strings deliberately NOT lifted" is a four-item list reported as
   three.** A fourth was lifted: *"Supplements at member price, only where we test the marker"*
   (`AppShell.tsx:951-954`). `lib/membership/includes.ts` holds the canonical list as **three**
   items and states the rule — *a paywall must not list a benefit that cannot be delivered*. The
   same file proves it cannot be: 250 lines later it records that every supplement in
   `lib/subscriptions/products.ts` is retired and points the shop row at a waitlist.
2. **Row 32c's Phase-0 qualifier was never propagated to `/demo`.** The row records the added
   sentence as *"the one finding the Guardrail #1 pre-flight produced"* against `/membership`;
   `/demo` renders the identical *"Ask the clinician"* benefit to the identical cold visitor with
   no qualifier (`AppShell.tsx:949`, `:1191-1192`).
3. **Row 32a asks one question where there are two.** The auto-renew ruling §4 authorises one
   sentence on four named `/kits/` routes, later extended to three `/lp/` pages. It says nothing
   about a standalone public `/membership` explainer, so that page is **not covered by the ruling
   at all** — row 32a inherits the ruling's authority for a surface outside its scope. This is the
   "compressed citation" failure `03_compliance/STATE.md` names. The question for Keith is not
   *which document wins* but **does a public priced membership page exist at all**.
4. **Row 32a's surface list omits `/demo`**, which prices the membership at `AppShell.tsx:920-926`
   and `:1143-1153` and which `site-funnel-model.md` §2 lists as an acquisition surface.
5. **`03_compliance/STATE.md`'s "twelve live sample-panel verdicts" is understated by four on the
   same page** — the `NEXT_STEP` badge words at `/lp/hormone-recovery:159-162` are the same defect
   class and were never counted, because the scanner rule was written against sample-report panels.
   `Suboptimal` is the word that entry itself singles out as *"appears on no retired-word list
   because nobody knew it was in use"*, and it is still in use, in a second place.
6. **A source comment on `/lp/testosterone:46` overstates its own defect**, saying *"two FAQ
   answers"* where there is one (`:63`). Row 42a says "an FAQ answer" and is correct.

**🟢 What the independent pass checked and cleared**, beyond the deterministic floor: ashwagandha
and magnesium both zero, including inside prohibition lists; EFSA wording exact-match across
nineteen named call sites; zero occurrences of "TRT" or "testosterone replacement" under
`(marketing)/`, `lp/` or `components/marketing/`; Kit 1 scoping correctly routes tiredness and
brain-fog to Kit 2 rather than claiming it; **the row 42 sample-panel extraction genuinely worked** —
every `ours:` value in `lib/kits/sampleReadout.ts` is `In range` or `Monitor`, both in `BADGES`,
`lab:` is `Lab normal` throughout and FAI draws no track; zero occurrences of the £75 deposit and
`JoinForm.tsx` deleted; CA-026 C1 byte-identical at `kits/page.tsx:599-602`; CA-018 consent and the
18+ gate unchanged and not conflated with health-data consent; row 12a independently verified as
still false and unchanged at `page.tsx:686`; the `MEMBERSHIP_ENABLED` interlock two-sided and sound
**as a gate**, though not as a phrase list; `/demo`'s data path carries no Supabase client and no
user id, so the homepage's *"We never put your data in it"* is structurally true; and the
prototype's invented *"next included retest is 12 November 2027"* was correctly not lifted.

---

### Row-by-row disposition, all 78 rows, 2026-09-15

Step 4 of the pre-flight procedure at the foot of this file. **Every row is marked. No row is
moved into a cleared state**, because clearing is a signature and this pass is not one — see the
note at the head of this section. The disposition says what is mechanically discharged and who owns
what remains, which is what a signer needs in front of them.

The split is driven by the file's own "Words changed?" column, per step 2 of the procedure:
**every Yes or Not yet is copy needing sign-off whatever the scanner says.**

| Disposition | Count | Rows |
| --- | --- | --- |
| 🟠 **Words changed — sign-off owed** | **26** | 4, 7, 12, 12a, 13, 14, 22, 23, 24, 25, 27, 29, 30, 30a, 31, 32, 32c, 33, 35d, 35e, 35f, 42a, 44, 45, 48, 48d |
| 🔴 **A ruling is missing** | **5** | 6, 10, 32a, 32d, 38a |
| ⚠ **Sequencing gate, not a copy item** | **2** | 11, 32b |
| ✅ **No words — deterministic floor passed, placement look owed** | **40** | 1, 2, 3, 5, 8, 9, 15, 16, 17, 18, 19, 20, 21, 26, 28, 32e, 34, 35, 35a, 35b, 35c, 37, 38, 38b, 38c, 39, 41, 42, 43, 43a, 44a, 45a, 45b, 46, 46a, 47, 47a, 48a, 48b, 48c |
| 🔵 **Nothing owed — recorded for context only** | **5** | 12b, 36, 39a, 40, 41a |

#### By owner, which is the list a signer actually needs

**🔴 EWA — clinical / claims.** Rows **22, 23** (marker values and verdicts changed on the sample
panels, re-derived from thresholds.md), **29, 30, 32c, 32e** (the CA-046 question set, now four rows
rather than three), **33** (the new `img-9` asset), **37** (the 999 line is now legible — she signed
the wording), and **48b** only if article copy ever changes, which it has not. Plus, from the
independent pass and not previously registered: the `/demo` compare toggle, *"and it did"*, the
fourth clinical-attribution variant, the notice that becomes false in compare mode, the three
non-`BADGES` verdict words, the four `/lp/hormone-recovery` badge words, and the 90-day cadence
statement. Plus three carried-from-`main` items the scanner surfaced: two `/faq` lines using
*treat* / *treatment* about NHS practice, two collagen support claims, and one dose-efficacy line.

**🟠 KEITH — business / framing.** Rows **10, 12a, 30a, 32a, 32d, 38a, 42a, 44a, 45, 45b, 46a, 48**.
Plus the **18** subscription-denial lines the widened interlock now sees (was 13 — see the
independent-pass section above). Plus, from the independent pass: the GP-receipt recombination, the
statutory-right framing, and the two universal comparative claims.

**🔵 CA-045 signers (Ewa + Keith), as scope notes before the packet is sent.** Rows **8, 18, 19,
33, 34**. ⚠ The packet as drafted describes ONE surface; the `HeroField` layer now renders on
**seven**. That is the single most important thing to fix before it goes out, and it is free now and
expensive after.

#### Two rows that changed status during this pass

- **Row 6 is no longer Ewa's, and it was never a clinical question.** Traced in full: her ruling is
  *"FAI — keep report-only, do not band in men"*, which governs BANDING and names no display word.
  "Not interpreted" is **Keith's K1 of 2026-08-12**; "Reported" is the engine label whose copy she
  approved on 2026-08-07 and confirmed at CA-034 E2. Both satisfy her ruling. The contradiction was a
  **mis-citation** in `kits/hormone-recovery/page.tsx` attributing the word to her and pointing at the
  wrong list item, plus `PRODUCT.md` calling it "invented". Both corrected 2026-09-15. What remains is
  a consistency call — should the two surfaces use one word — which is Keith's and is not a blocker.
- **Row 9 was structurally malformed** and is fixed: it had no trailing pipe, so its last two columns
  read shifted in any parse of this table. Nothing about its content changed.

#### The fragment pass (step 6 of the procedure), discharged

`fragment-scan.js` ran **once per article with an explicit `--source`**, the source named in each
run: the fragment is the article's `seoTitle` + `seoDescription` as served from
`blog_articles.frontmatter`, and the source is `content/blog/<slug>.mdx`, whose agreement with the
served body was confirmed by `sync-mirror.ts --check` immediately beforehand. Fifteen articles carry
SEO fields; **all fifteen returned 0 HARD.**

🟢 **Zero figure-not-in-source findings** — the check that matters most for a compression, since
compression is where a threshold gets rounded or misremembered. No snippet states a number its own
article does not.

The 32 REVIEW findings are all explicable and none needs action:

- **15 render obligations** — inapplicable by construction. An SEO snippet is text in a `<head>`; it
  never ships as an image, so there is no visual carrying claim weight. Recorded rather than waived
  silently, per the rule that a declared-but-unmet render obligation is worse than none.
- **16 qualifier drops** — 13 are graded *"hedges elsewhere"* or *"kept a qualifier of its own"*.
  ⚠ The 3 graded **"NOTHING on this slide hedges it"**, which the skill says is the one to act on,
  are **all SEO TITLES** (`fbc-blood-test`, `ferritin-blood-test`, `liver-function-blood-test`) — and
  all three are false positives of the tool's design. It matched each title against its article's
  opening sentence at 80–83% overlap and reported that sentence's hedges as dropped. A title is a
  LABEL, not a compressed claim: "Ferritin Blood Test: What Iron Stores Mean" asserts nothing that
  could be hedged. **Worth fixing in the tool** — a fragment with no verb-claim structure should not
  be graded for dropped qualifiers.
- **1 retest/efficacy term** — `"fixable"` in the `brain-fog` description. Carried from the
  **approved excerpt**, not introduced by the SEO commission, and since `main` falls back to the
  excerpt for the meta description it is already the live meta description today. The compression did
  drop *"a short list of"*, a slight broadening. → **Keith**, as metadata, per row 48c's ruling that
  an SEO snippet takes the metadata route rather than the clinical one.

---

## How the final pre-flight should use this

1. Read this table first, then run `/compliance-preflight` over the branch diff.
2. Treat every row marked **Yes** or **Not yet** in the "Words changed?" column as copy needing
   sign-off, whatever the scanner says: the scanner grades what is on the page and cannot know that
   a line was shortened from an approved original.
3. Row 6 is a blocker for any surface that renders an FAI verdict, and is independent of the
   redesign: it is wrong on `main` today.
4. Rows marked "placement only" still need a look, because CA-026 approvals attach to a claim **in
   a context**, and moving a claim past the buying decision can change what it reads as.
