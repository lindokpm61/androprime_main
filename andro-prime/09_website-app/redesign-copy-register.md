# Redesign copy register: what the final pre-flight has to pick up

_Last updated: 2026-09-09 (**Rows 38 to 40: the last eight content-marketing routes and both legal documents are Direction F. Conformance 16/36 to 26/36.** No claim, range, dose or EFSA sentence changed on any of them; `/privacy` and `/terms` assert that with a new test rather than with care, and row 39a removes a fallback that served a 200 with a legal heading and no legal document under it. Row 38a is a live copy defect found and deliberately NOT fixed: `/supplement-waitlist` promises a form control that does not exist. Earlier: **Row 37: a contrast fix on every dark panel site-wide, no words changed.** The SystemAlert action bar measured **2.39:1 against 4.5** on the line that says *call 999*; cause was specificity, and an audit across 16 routes found 12 failing text nodes in 4 places, now 0. Earlier: rows 35 to 36, **the blog rebuilt in Direction F**, which overturns the 2026-08-27 ruling; no article copy changed. Earlier still: rows 32 to 34, the public `/membership` page.)_

**The ruling this file exists to serve (Keith, 2026-09-02):** the redesign work on
`redesign/direction-f` is in test and **is not subject to copy pre-flight rules at each step**. The
pre-flight runs once, at the final stage, and picks up everything that needs changing or
re-approving. This is the same shape as the CA-045 ruling of 2026-09-01: **the gate governs
SHIPPING, not creating**, and this branch deploys nothing.

🔴 **THE RISK THE RULING CREATES, AND THE ONLY REASON THIS FILE EXISTS.** Deferring the gate is
fine. Deferring the gate with no record is not, because "we will catch it at the end" silently
becomes "we no longer know what changed". A pre-flight run against the finished branch can only
find what it can see: it reads the copy that is there, not the copy that was moved, shortened or
dropped along the way, and not the questions that were parked. So every step that touches
customer-facing words gets one line here, and the final pre-flight reads this file first.

**What belongs here:** copy moved, shortened, cut, or newly written; a claim whose container or
placement changed; an approved block that was relocated; a clinical or terminology question parked
rather than answered. **What does not:** pure design and layout changes that leave the words alone
(typography, spacing, containment, colour), which need no pre-flight at all.

---

## Open items

| # | Surface | What changed | Words changed? | Owed to |
| --- | --- | --- | --- | --- |
| 1 | `/kits` | The "what arrives in the post" tick list moves from three per-card copies to one line under all three cards. **Built 2026-09-02.** | **No.** Placement only, and it was identical on all three cards. | Pre-flight, sanity check only |
| 2 | `/kits` | **RESOLVED 2026-09-02 by tracing each orphaned field.** Five fields per kit came off the card. Three already have homes: `resultsTo` is absorbed by the new trust line; the conditional (`footLabel`/`footBody`) is covered by `/kits/testosterone`'s "The next step" plus its CA-026 D+ conformity line, and `/how-it-works` states the 12 nmol/L threshold three times; the blurb's ROUTING sentence ("Kit 2 is the better fit") is enforced harder at `/kits/testosterone:312`, a CA-025 scope block marked DO NOT REPOPULATE, and restated on `/kits` by the three parallel `who` lines. **Two are genuinely homeless: the blurb's explanatory half and `rightFor`.** Proposal: render `PANEL_MARKERS[id].why` in the panel alongside `measures`, which is existing approved copy that does the explanatory job better and is currently rendered nowhere on `/kits`; `rightFor` goes to the detail pages in the next pass. | **No new words** under the proposal: `why` is existing customer-facing copy in a new placement. | Pre-flight, placement check |
| 3 | `/kits` | **Built 2026-09-02.** The C1 "One price. Nothing hidden." panel moves from before the kit choice to after it. | **No.** CA-026 C1 carried verbatim, container and position only. | Pre-flight, placement check |
| 4 | `/kits` | **Built 2026-09-02.** Section 01 gains a standfirst: "Nine markers. Three ways to buy a slice of them. Every kit reads on the same lab, in the same units, so a result from one is comparable with a result from another." | **Yes, new.** Written for Frame O2. | Pre-flight |
| 5 | `/kits` | **Built 2026-09-02.** The panel instrument renders each marker's `measures` string from `lib/kits/panel.ts` in a new position (section 01 rather than inside cards). | **No.** Existing customer-facing strings, new placement. | Pre-flight, placement check |
| 6 | All kit surfaces | 🔴 **FAI verdict wording contradicts itself across two documents.** PRODUCT.md says "Not interpreted" was invented and the engine never returns it, and that the six allowed words come from the `BADGES` map, which yields "Reported". `/kits/hormone-recovery`'s header comment says the product's "Not interpreted" beats the frame's "Reported" because it is a clinical ruling (Ewa, `thresholds.md` item 8). Both cannot be current. | n/a, a ruling is missing | **Ewa.** Frame O2 renders no verdict, so it sidesteps rather than settles this |
| 7 | `/kits`, `/test-selector` | Known pre-existing drift, carried as found and flagged in Frame O: "answer 3 questions" and "takes less than a minute" both describe a selector that is now five steps. | **Not yet.** Fixing it is new copy. | Pre-flight |
| 8 | `/kits` | Kit card photographs `img-3`, `img-6`, `img-7` now appear on `/kits` as well as `/`. Already on the CA-045 register; this is a second placement of the same assets, not a new asset. | n/a | CA-045 signers, as a note |
| 9 | `/kits` | Each light card carries a one-line "who it is for", taken **verbatim from the homepage's own kit cards**, matched by slug ("If the question is testosterone." etc). | **No.** Existing live copy, new placement. | Pre-flight, placement check
| 10 | `/` (homepage) | 🔴 **Parked, not answered: whether that line owes the 30-day width of the offer window.** It is drafted without it, on the reading that the window is a TERM and belongs on the terms page, not in homepage fine print. The consequence of the omission is real: a man who declines and returns three months later finds no offer and was not told that could happen. | n/a, a ruling is missing | **Keith**, then pre-flight |
| 11 | `/` (homepage) | ⚠ **Row 12 describes a route that is currently dark.** `/membership` is behind `MEMBERSHIP_ENABLED`, which is unset, so it 404s. The line must not ship ahead of the flag, or the homepage promises an offer that cannot be made. | n/a, a sequencing gate | Whoever merges the branch |
| 12 | `/` (homepage) | **Built 2026-09-02.** The free layer gains a fine-print line naming the membership for the first time on an acquisition surface: "Holding that record over time is an optional membership. It is offered once your first result is back, never before, and you never need it to buy a kit or to read your own results." It exists because the section's approved lede ("what you pay for is the record") alludes to a charge this page is forbidden to sell, so the page named a paid thing and then went silent on it. Shape taken from the approved `/kits` C1 negation ("no subscription unless you choose one"); the words are new. No price, no CTA, no benefit list. | **Yes, new.** | Pre-flight |

| 12a | `/` (homepage) | 🔴 **ROW 12'S LINE IS NOW FALSE, AND NOTHING RE-OPENED ROW 12 WHEN IT BECAME FALSE. Owner: Keith, then pre-flight.** The line reads *"It is offered once your first result is back, never before, and you never need it to buy a kit or to read your own results."* That describes the opt-in model `01_strategy/2026-09-07-auto-renew-at-day-30.md` **superseded five days after the line shipped**: under the adopted ruling the first 30 days are **included in the kit price** and the card is **charged GBP 47 automatically on day 31**, with no re-consent step. So the homepage's only membership sentence tells a cold buyer the offer comes later and is optional, on the page whose kit cards are the entry to a purchase that now starts a subscription. It understates a recurring charge, which is the precise failure section 2 of that ruling cites the Numan ASA ruling for, and it is wrong **however row 32a resolves**: 32a governs whether the PRICE line ships, and this is a different sentence making a different claim. Found at wrap 2026-09-10, verified against `frontend/app/(marketing)/page.tsx:678` (unchanged since 2026-09-02) and against the absence of any "Cancel anytime" string outside `/membership`. **Owed: a rewritten line, then pre-flight.** ⚠ **Row 10 is superseded by this row**: its parked question was whether the line owes the 30-day window width, and the disclosure it actually owes is the renewal. | **Yes, a rewrite.** | **Keith**, then pre-flight |

| 13 | `/` (homepage) | **Built 2026-09-03.** Keith ruled one section grammar across the F pages and it is `/kits`' labelled one, so the homepage's four heading sections gain a `.f-blab` mono label each: **"The two ranges"** (02), **"Where to start"** (04), **"What's free"** (05), and **"No conflict"** (06, the `.f-blab-lg` lead label on the inverted receipt). Sections 01 and 03 open with an object and take no label. ⚠ Row 12's line sits in section 05, whose new label inherits its prohibitions: **"What's free" is a section name, not an offer**, and carries no price, no CTA, no benefit list, no "join", and does not lead on the trend or the series. ⚠ This row also **supersedes the standing note in two places** (`page.tsx` and `DESIGN.md`) that the invert panel's lead label was "deliberately absent pending a pre-flight"; it was a statement about what a redraw may do, and a critique later misread it as a design ruling. | **Yes, new.** Four labels, two to three words each. No claim in any of them. | Pre-flight |
| 12b | `/` (homepage) | ⚠ **Pointer correction, no copy change.** The disclosure line's own code comment says it is "registered as item 9"; the row describing it is **item 12**, and item 9 is the `/kits` "who it is for" lines. The comment was committed as found in `c746368` and corrected here rather than silently edited. | **No.** | Nothing owed |

| 14 | `/` (homepage) | **Built 2026-09-03.** The three kit cards said **"Testosterone"**, **"Energy & Recovery"** and **"Hormone & Recovery"**; every other surface says **"Testosterone Health Check"**, **"Energy & Recovery Check"** and **"Hormone & Recovery Check"** — `/kits`, the detail-page breadcrumbs, `lib/pricing.ts` (so the checkout), the account, the activation flow and the CIO payload. The homepage was the only dissenter, one click from the page it disagreed with. All three now read from `lib/kits/names.ts`, which already called itself the single source of truth and had **one** consumer. ⚠ The two narrow homepage cards' heading now wraps to two lines at 1440 (one line at 390); cards stay equal height, no overflow. | **Yes, changed** — but TO the already-approved long form, not to new words. The short forms were the unapproved variant. | Pre-flight, consistency check |
| 15 | `/kits` | **Built 2026-09-03.** The three card CTAs said **"Order"**; they now say **"Start a baseline"**, the homepage's label for the same action on the same three products. "Order" also over-promised: it leads to a product page, not a basket. | **No new words.** Existing live approved copy in a new placement. | Pre-flight, placement check |
| 16 | All kit surfaces | ⚠ **Not fixed, recorded so the pre-flight knows the scope.** The three product names have **66 hardcoded literal occurrences across 21 files**. Rows 14 and 15 route `/` and `/kits` through `lib/kits/names.ts`; the other 19 files still restate the strings, including `lib/pricing.ts`, `lib/account/getAccountData.ts`, `lib/account/exportResults.ts`, `lib/activate/getKitActivation.ts`, `lib/content/kitCTA.ts` and the three `/lp/*` pages. They all currently AGREE, which is exactly why the divergence was invisible until the homepage broke it. | n/a, a consolidation | Whoever picks up the naming sweep |

| 17 | `/kits` | **Built 2026-09-03, and it is here despite changing no words, deliberately.** Keith ruled one recommendation across both pages: **Kit 3 is the default**, so `/kits` moves from solid buttons on Kits 1 and 3 to a solid button on **Kit 3 alone**. Previously the two pages recommended different kits one click apart, and only the homepage had a commercial reason for its choice; `/kits`' was written in its code as a layout-balance rule. ⚠ **Why a design-only change is registered anyway:** it steers toward the **£179** product, and fair-presentation questions are asked of the whole presentation, not only of the words. The argument for it is coverage, not price (Kit 3 is 9 of 9 markers, and the homepage argues the full panel is the honest default for a reader who does not yet know his question), but the pre-flight should see it rather than discover it. | **No words changed.** One boolean per card. | Pre-flight, presentation check |

| 18 | `/kits` | **Built 2026-09-03, no words involved, registered because it moves an OPEN compliance question onto a second page.** The `HeroField` data layer now renders in the `/kits` hero as well as the homepage, so the two pages share one ground. It draws real range geometry from `04_products/results-engine/thresholds.md`, and **CA-045 questions 6 and 7 are open against exactly that** ("hero data field: display or texture", and "the hs-CRP and SHBG rows the page never shows"). Nothing is labelled, named or legible, and at 0.34 opacity with the per-row fade no individual band is readable, so the reading is texture-whose-source-is-real; that reading is not ours to ratify. Safe to build under the 2026-09-01 ruling that the gate governs SHIPPING not creating. ⚠ **What the answer has to cover has changed, and changed AGAIN on 2026-09-03**: the field now renders on `/`, `/kits`, `/kits/testosterone`, `/kits/energy-recovery` and `/kits/hormone-recovery`. **FIVE surfaces, and the packet as drafted describes one.** The three detail pages took it in the same pass that caught them up with the branch; they are the pages a reader arrives at with intent to buy, so if the reading of this layer is ever anything other than texture, these are the three surfaces where it matters most. | **No words.** | **CA-045 signers**, as a scope note before the packet is sent |

| 19 | `/kits/testosterone`, `/kits/energy-recovery`, `/kits/hormone-recovery` | **Built 2026-09-03.** The three kit detail pages were rebuilt in Direction F on 2026-08-29/31 and then stood still while `/` and `/kits` took four more commits, so they were Direction F pages a generation behind Direction F. Caught up in one pass: the shared `HeroField` ground, `.f-herogrid` in place of a raw Tailwind grid, `f-sec-hero` in place of an inline `paddingTop` (Kits 2 and 3 never got Kit 1's consent-banner fix, so at 390 their order buttons sat under the banner), the `SectionRule` spine on Kits 2 and 3 (which had NO measurement device while every other F page carried it), the `.f-pstrip` coverage strip on Kits 2 and 3, and Kit 1's containment move ported to Kit 2 (the opening argument left its tray). **Two photographs were placed**: `img-7` on Kit 2 and `img-3` on Kit 3, matched by slug to the assets those same two kits already use on `/` and `/kits`. Both are already on the CA-045 register; this is a third placement of the same assets, not a new asset. Alt text and captions are carried **verbatim from `/kits`** rather than re-derived, for the reason the 2026-09-02 packet established: `img-5`'s alt described the opposite of its photograph when it was written from a filename. | **No new words.** Existing approved strings in new placements, and one block of existing prose moved out of a card into the same page's grid. | Pre-flight, placement check; **CA-045 signers** as a note on row 18's widened scope |

| 20 | All three kit detail pages | **Built 2026-09-04.** The CA-026 D+ conformity line moves from a plain white `.f-tray > .f-core` into `.f-invert`, the ink panel `/` and `/kits` already use for the same argument. Measured before the change, `.f-invert` count across the five F routes was **4, 4, 0, 0, 0**: the claim PRODUCT.md names as the brand lead was in the loudest container on the two pages that sell nothing and the quietest on the three that take £99 to £179. Each page now spends its ONE permitted invert. ⚠ The sentence is split across `.f-invert-h` and `.f-invert-p` **at a sentence boundary with the order preserved**; Kit 2's split falls one sentence later than Kit 1's because its mono label IS its first sentence and splitting earlier set the identical string twice. A mechanical check now asserts every CA-026 sentence is still present in the rendered JSX on all three pages. | **No words changed, none added, none removed, none reordered.** Container and element split only. | Pre-flight, placement check |
| 21 | All three kit detail pages | **Built 2026-09-04.** Each hero gains a `.f-price` element reading `£{PRICING.KIT_N.rrp}` from `lib/pricing.ts`. Before this there was **no price element on any kit page**: `.f-price` / `.f-kprice` / `.f-prow-p` counted 3 on `/` and 6 on `/kits` and 0 here, so the number existed only as a substring inside the button label and in one FAQ answer, on the page that takes the money. The same price was measured in **8 distinct type treatments** across the five pages; it now renders at 35.2px/700 here and on `/kits`, from one class. | **No new words.** An existing number, from the existing source module, in a new element. ⚠ It sits directly above a button whose approved label already contains the price, so £N appears twice in the hero. Flagged rather than fixed: shortening the button label is a copy change. | Pre-flight, placement check |
| 22 | `/kits/energy-recovery` | 🔴 **Built 2026-09-04, and this is the row that needs the most attention.** The hero sample report is rebuilt from the single-bar `.f-rep` into the **two-range `.f-mk` / `.f-track` / `.f-band` / `.f-you` device the homepage uses**, because `/` opens on "Two ranges. Nine markers. You should see both" and the page that sells showed one bar, no lab band and no needle. **Three of the four rows are carried verbatim from `/`** — Vitamin D 58, Active B12 45, Ferritin 62 — values, band geometry and verdicts included, so they add no new clinical assertion. **hs-CRP is the one new row**, derived from thresholds.md (`≤1` normal, Vitall reference `<1.00 mg/L`, the match recorded at thresholds.md line 63) on a 0–10 scale; **its two bands coincide**, which is the weakest visual claim the device can make. ⚠ **Every row is lab-normal by construction**, so `f-v-lab` carries the string "Lab normal" on all four, byte-identical to `/`; a value the lab would call out-of-range would have needed a second lab-verdict string that exists nowhere in the approved set. ⚠ **The result is now MIXED (two In range, two Monitor), reversing an all-flagged demonstration** (Keith, 2026-09-04): the previous four rows read Action needed / Monitor / Monitor / Monitor, so the demonstration of the product was a man for whom nothing is fine. Both Monitors are genuine splits, so the page still shows the product finding what a standard report misses. The panel also **gains "Nothing here is a diagnosis"**, carried from `/`, which it did not previously carry while drawing bands and verdicts. | **Values changed** (Vitamin D 44→58, B12 61→45, hs-CRP 1.2→0.8, Ferritin 38→62) and **verdicts changed with them**, every one re-derived from thresholds.md and stated in the source comments. Marker sub-labels and the Recommendation line are unchanged. "Nothing here is a diagnosis" is existing approved copy in a new placement. | **Ewa**, as a clinical-demonstration change, and pre-flight |
| 23 | `/kits/testosterone`, `/kits/hormone-recovery` | 🔴 **RAISED AS A BLOCKER AND WITHDRAWN THE SAME DAY. Built 2026-09-04.** This row first said the two-range readout could not be drawn for Albumin and Free Testosterone because their rules are FLOORS rather than bands (`<35 -> GP` with the upper "left open"; `< referenceLow` with no upper action), so drawing a band meant inventing a bound Ewa had declined to set. **That was wrong, and Keith caught it by pointing at ClickUp.** The ruling exists in two places: `approval-record-biomarker-bands-v2-2026-08-07.md` records **"Albumin upper band | No | No change"**, which is a decision and not an omission; and **`lib/results/classifier.ts:resolveBarZones` implements it** as `{ color: 'optimal', upTo: null }` for both markers, has done since the 2026-06-16 sign-off, and **already renders that way to customers on the dashboard**. CA-044 records the bands themselves as APPROVED; what is still pending there is the card WORDING for `high-testosterone` and `high-vitamin-d`, which these rows do not touch. So a floor draws to the end of the track, our band renders wider than the lab's at the top on those two rows, and that is the ruling drawn honestly rather than a new claim. Kit 1 now shows 5 rows, 4 tracks, 1 split; Kit 3 shows 9 rows, 8 tracks, 3 splits; FAI draws no track on both (ruling 8). ⚠ **Two Kit 3 values changed to satisfy the lab-normal constraint**: Vitamin D 47 -> 58 and hs-CRP 2.1 -> 0.8, because 47 sits below Vitall's 50-250 interval and 2.1 above its <1.00, and "Lab normal" is the only lab-verdict string that exists in the app. A consequence worth Keith's eye: Kit 3's demonstration is now milder than it was, with no Action-needed row. | **Values changed on Kit 3** (Vitamin D, hs-CRP), each re-derived and stated in the source comments. No new strings; "Nothing here is a diagnosis" is carried from `/`. | **Ewa**, as a clinical-demonstration change, and pre-flight. The BAND GEOMETRY is already ratified and is not what is being asked. |

| 24 | All three kit detail pages | **Built 2026-09-06. NEW COPY, four short labels.** Keith's 2026-09-03 ruling is one section grammar across the F pages, and `/` and `/kits` label 4 of 4 heading-led sections while the kit pages left four bare. Added: **"Questions"** above the FAQ heading on all three, and **"The founders"** above Kit 3's founders section. Both are section NAMES in the existing grammar ("The reality", "The process", "The panel") and neither carries a claim, a benefit, a marker, a threshold or a number. Measured after: 0 unlabelled `h2.f-h2` on all five routes, at 1440 and 390. | **Yes, new.** Two distinct strings, one and two words. No claim in either. | Pre-flight |
| 25 | All three kit detail pages | **Built 2026-09-06. NEW COPY in placement, but TOWARDS the approved names.** The five F eyebrows read `none / "Diagnostic kits" / "Kit 01 // Testosterone" / "Data first" / "Data first"` — so two DIFFERENT products shared an eyebrow that identified neither, one click apart, and Kit 1's identified itself with a SHORT form. They now read **"Kit 1 // Testosterone Health Check"**, **"Kit 2 // Energy & Recovery Check"**, **"Kit 3 // Hormone & Recovery Check"**. Two fixes in one: the number is **unpadded**, matching `/kits`' own `NUMBER_LABEL` and the homepage cards (Kit 1 was the site's only "Kit 01"); and the name is **read from `lib/kits/names.ts`**, not typed, so this adds no new hardcoded call site to the 66 row 16 counts. ⚠ Row 14 records that the short forms ("Testosterone") were the **unapproved variant** and the long forms are the approved ones, so this moves Kit 1's eyebrow onto approved copy rather than away from it. Verified: none of the three wraps at 390. | **Changed, but TO the already-approved long form.** "Data first" is retired from two pages. | Pre-flight, consistency check |
| 26 | `/kits/hormone-recovery` | **Built 2026-09-06. Completes row 15's sweep.** The comparison table's footer CTAs for Kit 1 and Kit 2 said **"Order"** — the last two instances on the site. Row 15 removed that label from `/kits`' cards on 2026-09-03 for two reasons that both apply here: it is not the label `/` and `/kits` use for the same action on the same products, and it over-promises, because the link leads to a product page and not to a basket. Now **"Start a baseline"**, with the pip. Measured after: 0 "Order" CTAs across all five routes at both widths. | **No new words.** Existing approved copy replacing a label that row 15 already retired elsewhere. | Pre-flight, placement check |
| 27 | `/how-it-works` | **Built 2026-09-06. NEW COPY, one photo caption: "Any postbox, any morning".** The page had no photograph at all, so it had no caption convention to inherit; this follows the seven that exist ("Ordinary Tuesday", "Thursday, 4pm", "No email, no gate"): a place and a time, no claim, no product, no number. It sits under `img-8` in the hero. | **Yes, new.** Four words. No claim. | Pre-flight |
| 28 | `/how-it-works` | **Built 2026-09-06. Completes rows 15 and 26.** The three kit cards' CTAs said **"Order Kit 1 / 2 / 3"**. Those were the last three "Order" labels on the site and nobody had counted them, because the two earlier sweeps were scoped to `/kits` and the detail pages. All three now read **"Start a baseline"**, which is what `/` and `/kits` say for the identical link to the identical destination. | **No new words.** Existing live copy, matched by destination, replacing an unapproved variant. | Pre-flight, consistency check |
| 29 | `/demo` | **Built 2026-09-06. NEW COPY, the demonstration notice.** "These results are a sample. They do not belong to a real person, and no customer's data is used here. Everything else on this page is the report you would get: the same laboratory ranges, the same action bands, and the same explanations." Not dismissible, above the fold on every scenario, because a reader arriving from a shared link mid-page must still be able to tell whose numbers these are. The second sentence is the load-bearing one and it is a claim about the product, so it has to stay true: if the demo ever stops rendering through the real classifier, this line becomes false. | **Yes, new.** | **Ewa + Keith**, CA-046 q1 |
| 30 | `/demo` | 🔴 **REWRITTEN TWICE ON 2026-09-07; READ THE LAST PARAGRAPH FIRST, IT IS THE ONE THAT IS TRUE.** Originally: three result-switcher labels and blurbs.** It was three result-switcher labels and blurbs -- "Lab normal, three to monitor" / "Nothing to act on" / "A result that goes to a GP" -- argued as a set so the demo could not be read as the flattering case. Keith removed the switcher: two of the three were Kit 1, a five-marker panel on the page whose job is to show the product, and *"it's not needed in the demo page"*. **What is on the page now** is the JOURNEY switcher, three labels and three blurbs, which is copy this register has not previously carried: "Day 3 &middot; waiting for the lab", "Day 14 &middot; first result, month one running", "Day 104 &middot; member, retest landed", each with one line saying what that moment is. It names a **charge date and an included 30 days**, which is membership copy on an ungated surface and is the widening `03_compliance/STATE.md` already flags. The withdrawn strings, including the verbatim "That result earns us nothing", are no longer rendered anywhere on `/demo`. <br><br>🔴 **THEN THE WHOLE ROUTE WAS REBUILT FROM THE PROTOTYPE, and this row's real scope is now much larger than either version above.** Keith: *"rebuild the demo from scratch, following page for page the prototype. Exactly."* Twelve state-and-tab cells now render, and **the copy in them is lifted from `design/prototypes/demo-account-interactive.html`, which has had no pre-flight and no clinical sign-off and whose own header forbids lifting it to a live surface without both.** New strings, by screen: **waiting** -- "Where your sample is", "Your sample is being analysed", the four tracker step labels, "Ready for when it lands", "Why analysis takes days, not hours", "What a reference range is, and is not"; **plan** -- the month-one notice headline and its five inclusion bullets, "Your one number to move", "What month one includes, and what nothing gates"; **record** -- "Your record so far", "Only our own kits feed this", "Two purchases, one picture", "Your purchases"; **account** -- "GP handoff summary", "Ask the clinician", "Member shop", "Only where we test the marker"; **marker detail** -- "What the difference means", "Movement · two points, N days apart", the provenance line, and the attribution "Andro Prime clinical logic · signed by Dr Ewa Lindo". ⚠ **Three prototype strings were deliberately NOT lifted** and the substitutions are recorded in `components/app-shell/AppShell.tsx`: its invented daily tasks ("D3 capsule", "20 min daylight") and 1-to-10 energy scale, its own six verdict words, and its "Vitamin D3 + K2 · £18.95 member price" row, which names a product that does not exist in the range. | **Yes, new, and far more of it than either earlier version of this row described.** | **Ewa + Keith**, CA-046 q3 and the new q3b/q3c |
| 31 | `/`, `/demo` | **Built 2026-09-06.** The homepage's "See the app" card was inert with an "Opening soon" label because the route did not exist; it now reads **"Open the demo"** and links to `/demo`. The card's own body copy is unchanged and still says "We never put your data in it", which the route now makes structurally true: `lib/results/demo.ts` builds from fixtures with no Supabase client and no user id on the path. Also new: the demo's closing strip ("This is what you get") restating the finger-prick, the UKAS lab and the 2-to-5-working-day turnaround, all three already approved elsewhere. | **Yes, new** for the CTA label and the closing strip; the card body is unchanged. | Pre-flight |

| 32 | `/membership` (NEW public route) | 🔴 **Built 2026-09-08, and it is the largest single block of NEW COPY on the branch.** The seventh Direction F route: a PUBLIC explainer at `/membership` that discloses the membership price and the day-31 charge. It has **no join button and cannot have one** (membership may only be joined inside the 30 days after a result, enforced server-side with a 409), so every CTA routes to `/kits`. New strings, by section: hero (**"Your first 30 days are in the kit price. Then it renews."**, its standfirst, and the chip "No join button here"); the four-step spine (**"You buy a kit" / "Your result comes back" / "Your first 30 days run" / "Day 31"** with bodies and mono footers); section 02 (**"It starts when you learn something. Not when you pay."**, two paragraphs and the pull quote **"Ninety days after a card was charged is not ninety days after a baseline."**); section 03 (**"A test on a date, not a credit in an account."** and the 90/365 explanations); section 04, the ink panel (**"£47 a month, from day 31."** and two disclosure paragraphs); section 05 (**"Three things you keep whether you pay us or not."** and three items); section 06 (**"You cannot buy this on its own. On purpose."** and three paragraphs); six FAQ pairs; and the close (**"The test comes first."**). ⚠ **CARRIED VERBATIM, not new:** the three "what membership includes" lines and the entitlement paragraph ("Included while you are a member. You need to be a member on that date...") are lifted from the in-app paywall unchanged. ⚠ **Every number is imported, not typed** (`PRODUCT_MAP.membership`, `MEMBERSHIP_OFFER_WINDOW_DAYS`, `FIRST_CYCLE_RETEST_DAYS`, `ANNUAL_RETEST_DAYS`, `PRICING`, `KIT_NAMES`), so a constant moving breaks the build rather than the copy. 🔴 **Deliberately absent, each for a stated reason: member pricing on supplements** (no delivery path, the 2026-08-26 rule), **any kit discount** (there is none, and section 05 says so), and **"watch your numbers move" as the lead** (the §1b cold-start constraint). | **Yes, a lot, and all of it new.** | **Pre-flight**, as the largest single item on this register; and **Ewa + Keith** for anything read as a clinical cadence claim (the 90-day and 365-day intervals are stated to a customer on a marketing page for the first time, though both are already customer-facing inside the app) |
| 32a | `/membership`, `/kits/*` | 🔴 **THE TWO GOVERNING DOCUMENTS CONTRADICT EACH OTHER AND NEITHER WAS AMENDED. Owner: Keith.** `07_sales/funnel/site-funnel-model.md` §2 states an absolute prohibition, *"no acquisition surface may sell or price the membership"*, listed under "What no acquisition surface may do". Keith's ruling of 2026-09-07 (`01_strategy/2026-09-07-auto-renew-at-day-30.md` §4) requires the price line *"Includes 30 days of membership. £47/month after. Cancel anytime."* on all four `/kits/` routes, for a prominence reason the DMCCA read makes close to mandatory. The ruling is later and specific, so it was taken as governing for this build, **but the funnel model was never given a carve-out note and still reads as absolute.** The decision sweep of the same day did not catch it because the sweep hunts for superseded FACTS, and the prohibition is not superseded, only narrowed. What turns on the answer: whether this page may be linked from `/` or `/kits`, and whether the §4 price line ships at all. Logged as task-observer OBS-635 with a proposed sweep change. | n/a, a ruling is missing | **Keith**, then pre-flight |
| 32b | `/membership` | ⚠ **Sequencing gate, the same shape as row 11 and it is the reason the route is flag-gated.** `2026-09-07-auto-renew-at-day-30.md` §8: the moment the redesign merges, a page reading "£47/month after" would be live while **Stripe has no `trial_period_days` anywhere in the repo** and `createMembership` charges from checkout, so the mis-statement would arrive by MERGE rather than by anyone deciding to ship it. The page is behind `MEMBERSHIP_ENABLED` and 404s with the flag off, which is that document's own recommended fix. **The flag must not be flipped until the trial mechanic exists and the membership terms are drafted.** | n/a, a sequencing gate | Whoever flips the flag |
| 32c | `/membership` | 🔴 **NEW COPY, and it is the one finding the Guardrail #1 pre-flight produced against this page.** Item 3 of the includes list, *"Ask the clinician. Questions answered every month, published for all members."*, is carried verbatim from the in-app paywall, **but the surface changed**: in-app it is read by a member who has already met the product, and here it is read cold by a visitor who has never tested. `site-funnel-model.md` §2 forbids an acquisition surface implying clinical services are live, and the Phase 0 boundary in `03_compliance/CONTEXT.md` is the rule behind it. "Published for all members" already carries the general-not-individual point, but only by implication. Added, in the footer's own already-approved words: **"Clinician answers are general and published to every member; they are not individual medical advice and they do not replace your GP."** The verbatim line itself is unchanged. ⚠ **The underlying question is still open and is not settled by this sentence:** whether the benefit may be named on a public surface at all, given `membership-F.html` records that the live member screen has only ever rendered the block EMPTY. | **Yes, new**, one sentence, built from approved footer language. | **Ewa + Keith.** This is a Phase 0 boundary question, not a wording preference |
| 33 | `/membership` | **NEW ASSET, 2026-09-08: `img-9.jpg`** (gpt_image_2, 4:3, resized to 1600x1194 to match `img-8`). A plain paper wall calendar in a domestic kitchen with one date ringed in blue biro. ⚠ **It goes on the CA-045 register as a genuinely new asset**, the second after `img-8`. **Why this subject:** the eight existing photographs are seven men in domestic settings plus a postbox, and CA-045 q3/q4 both ask whether an ordinary man beside tiredness copy implies he is unwell; a ninth man would inherit that question. This frame has no person, no hands, no clinic, no blood, no sample, no product and no lettering of any kind (digits only, verified in the render), so it opens no judgement the packet is already asking. New caption: **"A date, written down"**, following the existing convention (a place and a time, no claim, no product, no number). | **Yes, new**: one alt string and one four-word caption. No claim. | **CA-045 signers** (Ewa + Keith), and pre-flight |
| 34 | `/membership` | ⚠ **Registered because it widens an OPEN compliance question to a SEVENTH surface, no words involved.** The shared `HeroField` data layer renders in this hero too. CA-045 q6/q7 are open against exactly that layer ("hero data field: display or texture"), and row 18 last counted five surfaces, then six with `/demo`. The packet as drafted describes one. | **No words.** | **CA-045 signers**, as a scope note before the packet is sent |

| 35 | `/blog`, `/blog/[slug]`, `/blog/preview/[slug]`, `/authors/[slug]` | 🔴 **THE BLOG IS REBUILT IN DIRECTION F, 2026-09-08, AND THIS OVERTURNS A STANDING RULING.** On 2026-08-27 Keith ruled AGAINST the first F blog: *"I think the old or the live blog style wins. There's a lot of detail missing from the F blog you created, which at the moment I don't know if you can capture."* `blog-F.html` (2026-08-29) was drawn as the answer and its own header says it *"does NOT overturn the 2026-08-27 ruling on its own... that ruling stands until Keith rules again."* **Keith ruled again on 2026-09-08: "rebuild it with the new website... make sure that it sticks to the design of the new website in both look and feel."** That is the second ruling the mockup was waiting for. | **No article copy changed.** Not one word of any of the 18 MDX bodies, frontmatter, FAQ, or reference list was touched. This row is here because the CONTAINERS every approved claim sits in have all changed. | Pre-flight, placement check across all 18 articles |
| 35a | Blog, all surfaces | **What the rebuild is, in one line: the twelve-piece editorial component system was preserved and re-expressed, not flattened.** That was the whole of Keith's objection. `styles/components/f-blog.css` (new, `fb-` namespace) gives F an emphasis ladder deep enough to hold all twelve: four rungs on RULE WEIGHT (2px hairline Caveat, 3px Note, 5px PullQuote, 8px Punchline), then four on GROUND (`--core` + hairline StatBox, `--core` + ink ring SystemAlert, tray + `--core` inner ClinicalInsight, tray + `--sunk` inner InlineKitCTA), then the inverted EvidenceBox at the top. Verified in the browser by reading computed background and border off every rendered piece and asserting no two share both. | **No words.** | Pre-flight, as evidence the detail survived |
| 35b | Blog, all surfaces | **What came off:** `.blog-skin` and the cream `#f4f4f0` ground, the 8px black frames, the 4px rules, `bg-dot-pattern`, `brutal-shadow`, the uppercase black sans headlines, the `text-stroke` wireframe hover on the closing CTA, and `.article-prose`. `styles/base/blog-skin.css` is **deleted**: it was a written licence to break two brand non-negotiables (the global box-shadow ban and the white-only marketing ground) and neither exception is needed now the token release permits radius and the ambient shadow site-wide. The ~130-line `.article-prose` block was removed from `globals.css` in the same change, once its last consumer moved. **The blog stops being a documented special case.** | **No words.** | Pre-flight, as a note |
| 35c | `/blog/[slug]` | ⚠ **THE PHOTOGRAPHS, and they are the one thing the mockup does not draw.** 10 of the 18 articles carry an Unsplash hero (`photoSrc`/`photoAlt`/`photoCredit`/`photoCreditUrl`); `blog-F.html` draws the article, the twelve pieces, the tail, the index and the author page, and **no photograph anywhere**. So the treatment is DERIVED from `.f-shot` in f-primitives rather than invented: same grayscale, same radius, same caption furniture, same hover de-saturation as every photograph on `/`, `/kits` and `/how-it-works`. 🔴 **The Unsplash ToS attribution is preserved verbatim and may not be removed**: visible "Photo by X on Unsplash" plus UTM on both links. The grayscale is also the brand mitigation, since stock photography is in the brand DON'T column. **No new image assets were generated**; these are the same 10 photos already live, re-framed. | **No words changed.** The credit line is the same sentence in a new container. | Pre-flight, placement check; **no new CA-045 item** |
| 35d | `/blog` (index) | **ONE STRUCTURAL CHANGE, registered because it is not a restyle.** The V2.0 index was a full-bleed alternating left/right stack: one article per band, headline at 3xl to 5xl, sides flipping each row, roughly one and a half articles visible at 1440. It is now a card grid, one lead card spanning the row then three-up, so about twelve articles occupy the space two used to. The category filter, pagination, featured/lead distinction, empty state and the real-photo-vs-generated-OG-card grayscale rule all survive. **New copy, small:** the index hero standfirst (*"What your numbers actually mean, what a reference range is and is not, and what the evidence says about moving one. Written plainly, reviewed by a GMC-registered GP."*), an `{n} articles` chip, and the closing block (*"A number you have watched move."* plus its sub). ⚠ The closing copy restates the site's existing argument and makes no new claim, but it is new wording. | **Yes, new**, three short blocks on the index only. | Pre-flight |
| 35e | `/blog/[slug]` | **New copy, small, all furniture rather than argument:** the FAQ heading became *"Frequently asked."* under a *"Common questions"* label (matching the F section grammar); the article closing CTA became *"Find out where you actually stand."* over the existing `KIT_PRICE_RANGE` line; the newsletter block became *"The occasional plain-English read."* under *"Not ready to test?"*. ⚠ The V2.0 closing CTA said *"System Directive: Baseline Check"* and the newsletter said *"Health Intelligence Newsletter"*; both are retired as brutalist voice that no longer matches the site. **The kit price range, the 2-to-5-working-day turnaround and the UKAS line are unchanged and still render from their existing sources.** | **Yes, new**, three headings and two sub-lines. No claim in any of them. | Pre-flight |
| 35f | Blog, component labels | **Two literals retired, registered because they were customer-visible strings.** `SysHeading` printed a `SYS:` chip before every heading that used it, and `BlogToc` printed `SYS: On this page`. Both are gone: a machine prefix on a page that no longer speaks that way. `References` was `System DB // References` and is now `References`. ⚠ `SysHeading` is used by exactly 1 article and `NumberedHeading` by 1, so the blast radius is small. **`PublishedEvidence` is used by ZERO articles** and was rebuilt anyway, since deleting an unused approved component is a separate decision. | **Yes, three strings retired.** | Pre-flight |
| 36 | Blog | ⚠ **Three rendering defects were found and fixed during the rebuild, all three by rendering rather than by reading, and all three predate it or arrived with it.** (1) **Every in-article kit CTA button rendered its label invisible** (ink on ink) on 17 of 18 articles, because the prose link rule out-specified the button class. (2) **The references list rendered as a ragged grid**, because `display:flex` on a markdown-authored `<li>` turns every child into a column; that rule was inherited from the V2.0 code, where it had never been exercised. (3) **The page mounted TWO back-to-top buttons**, one from `ArticleLayout` and one built into `ArticleToc`, overlapping at different scroll thresholds. Also fixed: article dates rendered raw ISO (`2026-08-07`) on the index and author page while the article header formatted them (`7 Aug 2026`); there is now **one** `formatArticleDate` in `lib/blog.ts` that all three read. | **No copy.** (1) to (3) are defects; the date change makes two surfaces agree with the third. | Nothing owed; recorded so the pre-flight knows the surfaces moved |

| 37 | Every dark panel, site-wide | ⚠ **CONTRAST FIX, 2026-09-08, no words changed. Registered because it altered rendered TEXT COLOUR on a compliance-bearing block.** Keith reported the SystemAlert action bar as unreadable. Measured: **2.39:1 against a 4.5 requirement**, on the line that reads *"sudden or severe chest pain? call 999. don't wait."* Cause was specificity, not colour choice: `.fb-alert p` (0,1,1) beat `.fb-alert-act` (0,1,0), so a light-card body rule painted the safety line `--ink-2` on an ink ground. **A GP-referral instruction that a reader cannot read is a safety issue before it is a design one**, which is why this is on the register rather than only in STATE. A headless contrast audit across 16 routes then found 12 failing text nodes in 4 distinct places, all now at 0. **No copy was edited, added, moved or removed on any surface.** | **No words.** Rendered colour only. | Pre-flight, as a note; ⚠ **worth Ewa's eye** that the 999 line is now legible, since she signed the wording |

## Closed

_None yet._

| 38 | `/about`, `/contact`, `/faq`, `/waitlist`, `/supplement-waitlist`, `/supplements`, `/supplements/daily-stack`, `/supplements/collagen` | 🔴 **THE EIGHT REMAINING CONTENT-MARKETING ROUTES ARE DIRECTION F, 2026-09-09.** Route conformance went 16/36 to 26/36 (44% to 72%). **No journey frame exists for any of them**, so layout was decided against DESIGN.md rather than ported from a drawing; every structural call is argued in each file's header. **What did NOT change: every claim, every range, every dose, every EFSA sentence, every FAQ answer, the CA-026 A1 standing claim on `/about` and the CA-026 C2 price answer on `/faq`.** All are byte-identical to the V2.0 pages. What DID change, and all of it is furniture: (a) **section labels**, because the F section grammar (`.f-blab` → `h2` → lede) is one label per section and the V2.0 pages had none; (b) **`/about`'s primary CTA**, "Choose your test" → **"See the tests"**, matching `/how-it-works` and `/membership` on the identical link to the identical destination, the same shape as row 28; (c) **`/supplements`, `/supplements/*`' eyebrow**, `Supplements // Launching Shortly` split into `.f-eyebrow` "Supplements" beside `.f-kchip` "Launching shortly", because the double slash is a V2.0 device and `.f-eyebrow` is one per page; (d) five short **close blocks** where the V2.0 page ended on a band with no ask. ⚠ Headline LINE BREAKS were re-placed on three heroes so no line orphans a word; not one word moved. | **No claim, range, dose or approved sentence changed.** New words are section labels and five close blocks, none carrying a claim. | Pre-flight, as a placement check across eight routes |
| 38a | `/supplement-waitlist` | 🔴 **A COPY DEFECT WAS FOUND AND DELIBERATELY NOT FIXED, BECAUSE FIXING IT IS A COPY CHANGE.** FAQ 4 answers *"Can I choose which product I want updates about?"* with *"Yes. The form lets you tell us whether you are interested in the Daily Stack, the Joint and Recovery Collagen, or both. You can change your mind later."* **The form on that page has no such control and never has.** `SupplementWaitlistForm` renders `interestedInProduct="any"` on this route and forwards it as a HIDDEN field; there is no product selector, and no route by which a reader could "change their mind later". It reads as true because `/supplements/daily-stack` and `/supplements/collagen` pass real values, so the FIELD exists in the payload while the CONTROL does not. Either the control gets built or the answer gets rewritten. **The answer is rendered unchanged pending that decision.** Logged as task-observer OBS-670. | n/a, an existing claim is wrong | **Keith**, then pre-flight |
| 38b | `/contact` | ⚠ **AN INTERACTION CHANGED: the FAQ is an open grid, not an accordion.** `FaqAccordion` is a V2.0 component with no F equivalent, and `/how-it-works`, `/membership` and every kit page render their questions open in `.f-faqgrid` (standardised by Keith on 2026-08-29). Four short answers behind four clicks, on a page whose own copy says *"worth checking before you write to us"*, is the accordion working against the sentence. **No answer text changed.** ⚠ Also removed: the five routing-card SVG icons and the Dr Ewa "Identity Verified" placeholder card. Direction F has no icon vocabulary (its one arrow is ruled to be a typographic glyph, "never a drawn SVG"), and a mono label reading "Identity Verified" over an outlined person glyph is a verification badge that verifies nothing, which is the device the 2026-09-03 ruling took off the footer chips. Dr Ewa is now `.f-initials`, as on `/kits/hormone-recovery`. | **No words changed** by either removal. | Pre-flight, as a note |
| 38c | `/faq` | ⚠ **THE STICKY JUMP NAV IS GONE; ITS SIX ANCHORS ARE NOT.** The V2.0 page pinned a second nav bar at `top-20` under the site nav, which is two fixed bars on a phone and a permanent bite out of the viewport on the longest page on the site. The same six links now sit once, in the hero, as `.f-kchip` anchors. **Every `id` is preserved**, so every inbound deep link still lands. ⚠ **And the page went from THREE full-bleed ink blocks to ONE.** DESIGN.md allows one inverted panel per page and reserves it for a conformity statement; `/faq` spends its one on **CA-026 C2, "Why do your tests cost more than a £45 test?"**, rendered verbatim. The NHS-gap section and the closing CTA lose their ink grounds and keep every word. 🔴 **The C2 answer is now a single constant read by BOTH the rendered block and the `FAQPage` JSON-LD**, which were two hand-maintained copies of one paragraph on the V2.0 page and could have drifted silently. | **No words changed.** | Pre-flight; ⚠ worth a look that C2 still reads as approved in an ink panel rather than a grey card |
| 39 | `/privacy`, `/terms` | 🔴 **THE TWO LEGAL DOCUMENTS ARE DIRECTION F, AND NOT ONE WORD CHANGED, MECHANICALLY ASSERTED.** Both routes are 28-line shells that inject 793 and 750 lines of V2.0 brutalist HTML from `canonical-site/*/index.html`. **Retyping them into JSX was rejected**: it puts a human transcription step between an approved legal document and what a customer reads, and nothing downstream would catch a dropped clause or a softened obligation. Instead `lib/legal/canonical.ts` rewrites **attributes only** and never touches a text node, and **`scripts/verify-legal-text.js` (new, in `npm test`) extracts every text node from the canonical source and from the transform's output and fails the build if they differ by one character.** Both guards were mutation-tested: break the transform on purpose, confirm red, restore. Current: privacy 265 text nodes identical, outline h1/h2/h3 = 1/13/8; terms 215 identical, 1/15/6. **What the documents lost: their ink panels, their eighteen SVG icons and their `border-b-4` frames.** Direction F allows one inverted panel per page for a conformity statement, has no icon vocabulary, and gives prose no container at all; a privacy policy is 793 lines of prose. Headings keep their levels and their order, so the outline, the anchors and screen-reader navigation are unchanged. | **NO WORDS CHANGED, and this is the one row on this register where that is asserted by a test rather than by care.** | Pre-flight, as a note only; **no re-approval owed** |
| 39a | `/privacy`, `/terms` | 🔴 **A SILENT-FAILURE PATH WAS REMOVED, AND IT IS A COMPLIANCE FIX RATHER THAN A RESTYLE.** Both route files ended with a fallback returning `<h1>Privacy Policy</h1>` (or Terms) above the sentence *"The canonical privacy content could not be loaded."* So a read failure or a changed anchor served a **200 with a real legal-page heading and no legal document under it**, and the only signal was one sentence a reader had to notice and act on. A privacy policy that silently is not there is a UK GDPR Article 13 failure wearing the costume of a working page; terms a customer is told they agreed to, absent from the page they were pointed at, is a consumer-law problem before it is a bug. `getLegalDocument` now **throws**, which surfaces as a build failure or a 500. ⚠ Also collapsed: the two routes used **different extraction regexes** for one job, and the terms one ran PAST `</main>` and swallowed everything up to the footer comment. One window now, for both. | **No words.** A failure mode changed. | Nothing owed; recorded because the failure it removes is a compliance one |
| 40 | `/lp/collagen`, `/lp/daily-stack` | ⚠ **A SHARED COMPONENT WAS ABOUT TO DRAG TWO UNREBUILT ROUTES INTO DIRECTION F, AND WAS STOPPED.** `SupplementWaitlistForm` has six call sites: the four supplement routes rebuilt above, and these two `/lp/*` pages, which are still V2.0 and are **blocked from rebuild behind the 2026-09-07 auto-renew ruling**. Restyling the component outright moved the seam rather than closing it: the conformance run immediately afterwards showed both `/lp/*` routes jumping from 0 F classes to 10, i.e. a Direction F card sitting inside a brutalist page nobody has reviewed. Fixed with a `variant` prop **defaulting to `'v2'`**, so both routes render byte-identical markup to what shipped, and the four F pages opt in. This is the ruled `RelatedArticles` fix from DESIGN.md gap 4 applied to a second component: *"the host declares its own world, which is the one thing the host reliably knows."* Re-measured after: both `/lp/*` back to 0. | **No words on any surface.** | Nothing owed; recorded so the `/lp/*` rebuild knows the variant exists |

---

## How the final pre-flight should use this

1. Read this table first, then run `/compliance-preflight` over the branch diff.
2. Treat every row marked **Yes** or **Not yet** in the "Words changed?" column as copy needing
   sign-off, whatever the scanner says: the scanner grades what is on the page and cannot know that
   a line was shortened from an approved original.
3. Row 6 is a blocker for any surface that renders an FAI verdict, and is independent of the
   redesign: it is wrong on `main` today.
4. Rows marked "placement only" still need a look, because CA-026 approvals attach to a claim **in
   a context**, and moving a claim past the buying decision can change what it reads as.
